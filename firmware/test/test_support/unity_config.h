/* Empty Unity config: PlatformIO's test runner defines
 * UNITY_INCLUDE_CONFIG_H, which forces unity_internals.h to pull this
 * header in. We rely on Unity's defaults, so the file intentionally has
 * no content beyond this comment. */
#ifndef BIBA_UNITY_CONFIG_H
#define BIBA_UNITY_CONFIG_H
#endif
