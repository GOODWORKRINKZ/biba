# biba_camera (hook-point)

> FPV-stream и опциональный ML inference. Пустая директория с `COLCON_IGNORE`.

Целевая роль: захват видео с подключённой камеры, публикация `image_raw`/`compressed`, опционально inference на CPU/NPU. Требует Pi 4/5 или равнозначного SBC.
