# biba_uwb_follow (hook-point)

> Follow-the-tag через BU4. Пустая директория с `COLCON_IGNORE`.

Целевая роль: чтение BU4 по USB/UART, публикация pose тага и/или прямо `cmd_vel` с низким приоритетом в `twist_mux`.
