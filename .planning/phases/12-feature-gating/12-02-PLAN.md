---
phase: 12-feature-gating
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - firmware/src/modes/mode_standalone.c
autonomous: true
requirements:
  - FEAT-02
  - FEAT-05

must_haves:
  truths:
    - "Каждая фича в mode_standalone.c защищена #if BIBA_FEATURE_<NAME> на месте вызова"
    - "Когда тумблер выключен, код пропускает вызов фичи и использует default-safe значения"
    - "BIBA_FEATURE_RPM_CLOSED_LOOP=0 производит поведение, идентичное старому BIBA_OPEN_LOOP"
    - "Старый #ifndef BIBA_OPEN_LOOP / #ifdef BIBA_OPEN_LOOP полностью заменён на #if BIBA_FEATURE_RPM_CLOSED_LOOP"
    - "Микшер без L∞ projection использует biba_mix_differential() — функцию, которая уже существует в control_loop.h но никогда не вызывалась"
    - "BIBA_REVERSE_PIP_ENABLED переименован в BIBA_FEATURE_REVERSE_PIP (pattern: #if → #if)"
    - "Blackbox record поля gated: rpm_hz10 и pi_integral_* заполняются нулями когда соответствующие фичи выключены"
  artifacts:
    - path: "firmware/src/modes/mode_standalone.c"
      provides: "Все 17 feature gates на call sites + default-safe значения"
      contains: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_ZC"
      contains: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_SPECTRAL"
      contains: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_LOAD_GATE"
      contains: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_ANTI_STALL"
      contains: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_DUAL_WINDOW"
      contains: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_DR"
      contains: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI"
      contains: "#if BIBA_FEATURE_LATCH_RECOVERY"
      contains: "#if BIBA_FEATURE_CURRENT_LIMITER"
      contains: "#if BIBA_FEATURE_STEERING_DEADBAND"
      contains: "#if BIBA_FEATURE_HEADING_HOLD"
      contains: "#if BIBA_FEATURE_SPEED_MODE"
      contains: "#if BIBA_FEATURE_MIXER_PROJECTION"
      contains: "#if BIBA_FEATURE_RPM_RAMP"
      contains: "#if BIBA_FEATURE_MELODY"
      contains: "#if BIBA_FEATURE_REVERSE_PIP"
      contains: "biba_mix_differential"
  key_links:
    - from: "mode_standalone.c on_adc_pair_done()"
      to: "biba_config.h feature toggles"
      via: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_*"
      pattern: "#if BIBA_FEATURE_"
    - from: "mode_standalone.c tick() L∞ ball mixer"
      to: "control_loop.h biba_mix_differential()"
      via: "#else branch of BIBA_FEATURE_MIXER_PROJECTION"
      pattern: "biba_mix_differential"
    - from: "mode_standalone.c blackbox record"
      to: "gated variables"
      via: "#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI"
      pattern: "rec\.pi_integral"
    - from: "old #ifndef BIBA_OPEN_LOOP"
      to: "new #if BIBA_FEATURE_RPM_CLOSED_LOOP"
      via: "Search-and-replace: #ifndef → #if, #ifdef → #if !"
      pattern: "#if BIBA_FEATURE_RPM_CLOSED_LOOP"
---

<objective>
Внедрить #if BIBA_FEATURE_<NAME> guards в mode_standalone.c на всех точках вызова фич: ISR (on_adc_pair_done), init, tick, blackbox. Заменить старые #ifndef BIBA_OPEN_LOOP блоки на каскад индивидуальных тумблеров. Переименовать BIBA_REVERSE_PIP_ENABLED.

Purpose: Каждая фича цепочки CRSF→PWM становится независимо отключаемой без побочных эффектов. Отключение мастера RPM_CLOSED_LOOP даёт поведение, идентичное старому BIBA_OPEN_LOOP.
Output: mode_standalone.c с гранулярными #if guards, default-safe behaviour для всех disabled фич.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-feature-gating/12-01-PLAN.md
@.planning/phases/12-feature-gating/12-RESEARCH.md    # Sections 3.1-3.4: exact line numbers for every gate
@firmware/src/modes/mode_standalone.c                   # Full signal chain

<interfaces>
<!-- Key patterns from current mode_standalone.c for executor reference -->

Architecture: mode_standalone.c (1277 lines) — the ONLY file modified.
Three main functions: on_adc_pair_done() ISR, biba_mode_standalone_init(), biba_mode_standalone_tick().

Current #ifndef BIBA_OPEN_LOOP block locations:
  - Lines 388-490: ISR RPM chain (ZC, spectral, load_gate, anti-stall, dual_window, DR, PI)
  - Lines 927-1004: tick RPM ramp + PI duty assignment + direction-flip logic
  - Line 1176-1178: blackbox rpm_hz10 field gating (#ifdef BIBA_OPEN_LOOP → rpm=0)

Old BIBA_REVERSE_PIP_ENABLED pattern:
  - Lines ~1080: #if BIBA_REVERSE_PIP_ENABLED (value check — correct pattern, just rename)

Gate-at-call-site pattern (RESEARCH.md 10.1):
  ALL #if guards go in mode_standalone.c at the function call point.
  Do NOT add guards inside module .c files (zc_detector.c, rpm_pi.c, etc.)

Default-value pattern (RESEARCH.md 10.2):
  When a feature is OFF, explicitly set safe defaults:
  - Floats → 0.0f
  - Structs → {0} (zero-initialized)
  - Bools → false
  - Ints → 0u

Flat gate pattern (RESEARCH.md Pitfall 6):
  Use flat sequential #if/#else/#endif, NOT nested:
  GOOD: #if A ... #endif / #if B ... #endif
  BAD:  #if A ... #if B ... #endif ... #endif

Existing function to use when MIXER_PROJECTION=0:
  `biba_mix_differential(throttle, steering)` — defined in control_loop.h line 91,
  implemented in control_loop.c line 120-127. Returns biba_mix_output_t {.left, .right}.
  Does `biba_clamp_unit(throttle ± steer)`. Currently NEVER CALLED in the codebase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ISR RPM chain gates (on_adc_pair_done, lines 388-490)</name>
  <files>firmware/src/modes/mode_standalone.c</files>
  <action>

Transform the single `#ifndef BIBA_OPEN_LOOP ... #endif` block (lines 388-490) into individual flat gates for each RPM sub-feature.

**CRITICAL: READ the actual code at lines 388-490 before editing.** The line numbers below are from RESEARCH.md and should be verified against the actual file. The gate logic description is WHAT to gate, not exact copy-paste of existing code.

**Gate 1: RPM_ZC (currently lines 389-393)**

Replace the ZC calls:
```c
// OLD (inside #ifndef BIBA_OPEN_LOOP):
zc_left = zc_freq_analyze(s_adc_left_buf, samples_per_channel, STANDALONE_RPM_WHEEL_SPS);
zc_right = zc_freq_analyze(s_adc_right_buf, samples_per_channel, STANDALONE_RPM_WHEEL_SPS);
```

With:
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_ZC
    zc_left = zc_freq_analyze(s_adc_left_buf, samples_per_channel, STANDALONE_RPM_WHEEL_SPS);
    zc_right = zc_freq_analyze(s_adc_right_buf, samples_per_channel, STANDALONE_RPM_WHEEL_SPS);
#else
    /* zc_left/zc_right already zero-initialized at declaration (line ~378) */
#endif
```

**Gate 2: RPM_SPECTRAL (currently lines 395-400)**

```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_SPECTRAL
    spec_left = biba_rpm_spectral_estimate(s_adc_left_buf, samples_per_channel, STANDALONE_RPM_WHEEL_SPS, s_meas_target_hz_left, s_hint_hz_left);
    spec_right = biba_rpm_spectral_estimate(s_adc_right_buf, samples_per_channel, STANDALONE_RPM_WHEEL_SPS, s_meas_target_hz_right, s_hint_hz_right);
#else
    /* spec_left/spec_right already zero-initialized at declaration */
#endif
```

**Gate 3: RPM_LOAD_GATE (currently line 402)**

```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_LOAD_GATE
    biba_rpm_spectral_apply_load_gate(&spec_left, &spec_right);
#endif
```
(No else needed — function mutates spec structs in place; when skipped, spec structs retain their values from spectral call or zero-init.)

**Gate 4: RPM_ANTI_STALL (currently lines 404-427)**

Wrap the entire `if (s_meas_left_enabled) { ... }` + `if (s_meas_right_enabled) { ... }` anti-stall blocks (lines 404-427):
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_ANTI_STALL
    if (s_meas_left_enabled) {
        /* ... existing anti-stall left code ... */
    }
    if (s_meas_right_enabled) {
        /* ... existing anti-stall right code ... */
    }
#else
    s_antistall_duty_left  = 0.0f;
    s_antistall_duty_right = 0.0f;
#endif
```

NOTE: s_antistall_cnt_left/right are not explicitly zeroed in #else (they're state variables declared at file scope and initialized to 0 at program start — sufficient for compile-time disable).

**Gate 5: raw_hz assignment (currently lines 428-429 — uses zc_left/right)**

These lines are OUTSIDE the current #ifndef BIBA_OPEN_LOOP block:
```c
raw_hz_left  = s_meas_left_enabled  ? zc_left.freq_hz  : 0.0f;
raw_hz_right = s_meas_right_enabled ? zc_right.freq_hz : 0.0f;
```
Keep these lines UNCHANGED. When RPM_ZC=0, zc_left/right are zero-initialized so freq_hz=0.0f — behaviour is correct.

**Gate 6: RPM_DUAL_WINDOW (currently lines 430-437 — hint_hz update)**

```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_DUAL_WINDOW
    if (s_meas_left_enabled && spec_left.valid && spec_left.invalid_reason == BIBA_RPM_SPECTRAL_INVALID_NONE) {
        s_hint_hz_left = spec_left.freq_hz;
    }
    if (s_meas_right_enabled && spec_right.valid && spec_right.invalid_reason == BIBA_RPM_SPECTRAL_INVALID_NONE) {
        s_hint_hz_right = spec_right.freq_hz;
    }
#endif
```
(No else needed — s_hint_hz_left/right are file-scope static, initialized to 0.0f at init and by DR reset.)

**Gate 7: RPM_DR (currently lines 438-451 — the compound block `{ ... biba_rpm_dr_update ... }`)**

Wrap the ENTIRE compound block (from the opening `{` for dr_reason_left declaration through zc_ema_update and all telemetry variable assignments, ending at the closing `}`):
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_DR
    {
        /* ... existing DR block ... */
    }
#else
    spec_hz_left  = s_meas_left_enabled  ? spec_left.freq_hz  : 0.0f;
    spec_hz_right = s_meas_right_enabled ? spec_right.freq_hz : 0.0f;
    pi_meas_hz_left  = s_meas_left_reverse  ? -spec_hz_left  : spec_hz_left;
    pi_meas_hz_right = s_meas_right_reverse ? -spec_hz_right : spec_hz_right;
    /* Telemetry variables zeroed — matching open-loop equivalent */
    s_spec_quality_left   = 0.0f;
    s_spec_quality_right  = 0.0f;
    s_spec_peak_left      = 0.0f;
    s_spec_peak_right     = 0.0f;
    s_spec_candidate_left  = 0.0f;
    s_spec_candidate_right = 0.0f;
    s_spec_reason_left    = 0u;
    s_spec_reason_right   = 0u;
    s_freqdet_blocks_left  = 0u;
    s_freqdet_blocks_right = 0u;
    s_freqdet_cross_left   = 0u;
    s_freqdet_cross_right  = 0u;
    s_freqdet_pkpk_left    = 0u;
    s_freqdet_pkpk_right   = 0u;
    s_freqdet_std_left     = 0.0f;
    s_freqdet_std_right    = 0.0f;
#endif
```

**Gate 8: RPM_PI (currently lines 475-480 — biba_rpm_pi_step calls + anti-stall override)**

Wrap PI step calls AND the anti-stall override:
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI
        s_rpm_duty_left  = biba_rpm_pi_step(&s_rpm_pi_left,  &s_rpm_cfg, s_target_hz_left,  pi_meas_hz_left);
        s_rpm_duty_right = biba_rpm_pi_step(&s_rpm_pi_right, &s_rpm_cfg, s_target_hz_right, pi_meas_hz_right);
        /* Anti-stall override */
        if (s_antistall_duty_left > 0.0f && s_meas_left_enabled) {
            float boosted = s_rpm_duty_left + s_antistall_duty_left;
            if (boosted > s_rpm_duty_left) s_rpm_duty_left = boosted;
        }
        if (s_antistall_duty_right > 0.0f && s_meas_right_enabled) {
            float boosted = s_rpm_duty_right + s_antistall_duty_right;
            if (boosted > s_rpm_duty_right) s_rpm_duty_right = boosted;
        }
#else
        s_rpm_duty_left  = 0.0f;
        s_rpm_duty_right = 0.0f;
#endif
```

**Gate 9: LATCH_RECOVERY (currently lines 500-550 — ISR context)**

Wrap latch detection logic in ISR (between PI block end and tick start):
```c
#if BIBA_FEATURE_LATCH_RECOVERY
    /* ... existing latch detection code (lines 500-550) ... */
#else
    s_latch_reset_pending = false;
#endif
```

**REMOVE the outer `#ifndef BIBA_OPEN_LOOP` / `#endif` wrapper.** After inserting all individual gates, delete the old wrapper so nothing is double-guarded.

**IMPORTANT:** The `s_meas_raw_hz_*`, `s_spec_hz_*`, `s_pi_meas_hz_*`, `s_meas_hz_*` assignments AFTER the `#endif /* !BIBA_OPEN_LOOP */` (currently lines 492-498, outside the old guard) stay UNCHANGED and UNGUARDED. These always run — they just record telemetry values (which will be 0.0f when features are off — correct behavior).

  </action>
  <verify>
    <automated>
# Verify old guards are removed
cd firmware && ! grep -c '#ifndef BIBA_OPEN_LOOP' src/modes/mode_standalone.c; echo "exit: $?"

# Verify new guards exist
cd firmware && grep -c '#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_ZC' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_SPECTRAL' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_DR' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_LATCH_RECOVERY' src/modes/mode_standalone.c

# Build default config
cd firmware && pio run -e rpico_rp2040_standalone 2>&1 | grep -E "SUCCESS|FAILED"
    </automated>
  </verify>
  <done>
    - ISR: все 9 feature gates (ZC, SPECTRAL, LOAD_GATE, ANTI_STALL, DUAL_WINDOW, DR, PI, ANTI_STALL override, LATCH_RECOVERY) внедрены с default-safe #else
    - Старый #ifndef BIBA_OPEN_LOOP wrapper удалён
    - Default build (все тумблеры=1): SUCCESS
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate init + tick functions — safety, comfort, drive toggles</name>
  <files>firmware/src/modes/mode_standalone.c</files>
  <action>

Add #if BIBA_FEATURE_<NAME> guards in biba_mode_standalone_init() and biba_mode_standalone_tick().

**PART A — biba_mode_standalone_init() (lines 577-622):**

**A1. Gate PI config init (currently lines 588-597):**
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI
    s_rpm_cfg.kp             = BIBA_RPM_PI_KP;
    s_rpm_cfg.ki             = BIBA_RPM_PI_KI;
    s_rpm_cfg.ki_low         = BIBA_RPM_PI_KI_LOW;
    s_rpm_cfg.ki_low_thresh  = BIBA_RPM_PI_KI_LOW_THRESH;
    s_rpm_cfg.ff_slope       = BIBA_RPM_PI_FF_SLOPE;
    s_rpm_cfg.ff_dead        = BIBA_RPM_PI_FF_DEAD;
    s_rpm_cfg.stiction_floor = BIBA_RPM_PI_STICTION;
    s_rpm_cfg.p_clamp        = BIBA_RPM_PI_P_CLAMP;
    s_rpm_cfg.dt_s           = BIBA_RPM_PI_DT_S;
    biba_rpm_pi_reset(&s_rpm_pi_left);
    biba_rpm_pi_reset(&s_rpm_pi_right);
#endif
```

**A2. Gate DR init (currently line 599-600):**
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_DR
    biba_rpm_dr_reset(&s_dr_left);
    biba_rpm_dr_reset(&s_dr_right);
#endif
```

**A3. Gate hint_hz init (currently lines 601-602):**
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_DUAL_WINDOW
    s_hint_hz_left  = 0.0f;
    s_hint_hz_right = 0.0f;
#endif
```

**A4. Gate RPM ramp init (currently lines 602-603):**
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_RAMP
    biba_ramp_init(&s_rpm_setpoint_ramp_left);
    biba_ramp_init(&s_rpm_setpoint_ramp_right);
#endif
```

**A5. Gate s_rpm_duty init (currently lines 604-605):**
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI
    s_rpm_duty_left   = 0.0f;
    s_rpm_duty_right  = 0.0f;
#endif
```
Note: s_rpm_duty_left/right are file-scope static — already zero. This gate just skips the assignment.

**A6. Gate MELODY startup (currently line 617):**
```c
#if BIBA_FEATURE_MELODY
    biba_melody_player_start(&s_player, &biba_melody_startup);
#endif
```

**PART B — biba_mode_standalone_tick():**

**B1. Gate SPEED_MODE (currently lines 790-797):**
Replace the hardcoded speed_scale logic:
```c
#if BIBA_FEATURE_SPEED_MODE
    float speed_scale;
    if (speed_sel < BIBA_SPEED_MODE_LOW_THRESHOLD) {
        speed_scale = BIBA_SPEED_MODE_SLOW_SCALE;
    } else if (speed_sel > BIBA_SPEED_MODE_HIGH_THRESHOLD) {
        speed_scale = BIBA_SPEED_MODE_FAST_SCALE;
    } else {
        speed_scale = BIBA_SPEED_MODE_MEDIUM_SCALE;
    }
#else
    float speed_scale = 1.0f;
#endif
```

**B2. Gate STEERING_DEADBAND (currently lines 815-820):**
```c
#if BIBA_FEATURE_STEERING_DEADBAND
    float steering;
    {
        float mag = raw_steering < 0.0f ? -raw_steering : raw_steering;
        if (mag <= BIBA_STEERING_DEADBAND)
            steering = 0.0f;
        else
            steering = raw_steering < 0.0f ? -(mag - BIBA_STEERING_DEADBAND)
                                            :  (mag - BIBA_STEERING_DEADBAND);
    }
#else
    float steering = raw_steering;
#endif
```

**B3. Gate HEADING_HOLD (currently lines 824-830):**
```c
#if BIBA_FEATURE_HEADING_HOLD
    if (stabilized && armed) {
        float correction = biba_pid_step(&s_heading_pid, &s_heading_cfg, 0.0f, dt);
        steering = biba_clamp_unit(steering + correction);
    } else if (!armed) {
        biba_pid_reset(&s_heading_pid);
    }
#else
    (void)stabilized; /* suppress unused-variable warning when heading hold off */
#endif
```

**B4. Gate MIXER_PROJECTION (currently lines 898-914 — L∞ ball math):**
Replace the inlined L∞ ball code:
```c
#if BIBA_FEATURE_MIXER_PROJECTION
        float t = biba_clamp_unit(throttle);
        float s = biba_clamp_unit(steering);
        float l_raw = t + s;
        float r_raw = t - s;
        float al = l_raw < 0.0f ? -l_raw : l_raw;
        float ar = r_raw < 0.0f ? -r_raw : r_raw;
        float peak = al > ar ? al : ar;
        float denom = peak > 1.0f ? peak : 1.0f;
        biba_mix_output_t mix;
        mix.left  = l_raw * speed_scale / denom;
        mix.right = r_raw * speed_scale / denom;
#else
        biba_mix_output_t mix = biba_mix_differential(throttle, steering);
        mix.left  = biba_clamp_unit(mix.left  * speed_scale);
        mix.right = biba_clamp_unit(mix.right * speed_scale);
#endif
```
NOTE: `biba_mix_differential()` is defined in control_loop.h line 91. It does `biba_clamp_unit(throttle ± steer)`. Apply speed_scale post-mix with clamp. Per CONTEXT.md Specific Ideas: "without L∞ ball projection, but with сохранением speed_scale".

**B5. Gate CURRENT_LIMITER (currently lines 915-921):**
```c
#if BIBA_FEATURE_CURRENT_LIMITER
        biba_limit_result_t out = apply_drive_current_limits(mix);
        left_limited  = out.left_limited;
        right_limited = out.right_limited;
        left_out  = out.left;
        right_out = out.right;
#else
        left_out  = mix.left;
        right_out = mix.right;
#endif
```

**B6. Gate RPM_RAMP in tick (currently lines 927-943 — SECOND #ifndef BIBA_OPEN_LOOP block):**

Replace the outer guard (lines 927: `#ifndef BIBA_OPEN_LOOP`) with:
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_RAMP
    if (armed && !(s_dbg_active && s_dbg_open_loop)) {
        left_out = biba_ramp_update_with_rates(&s_rpm_setpoint_ramp_left, left_out, dt, ...);
        right_out = biba_ramp_update_with_rates(&s_rpm_setpoint_ramp_right, right_out, dt, ...);
    } else {
        biba_ramp_reset(&s_rpm_setpoint_ramp_left);
        biba_ramp_reset(&s_rpm_setpoint_ramp_right);
    }
#endif
```

**B7. Gate RPM_PI in tick (currently lines 948-999 — steering balance + bidirectional RPM logic):**

Replace the PI duty assignment block:
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI
    /* Steering integral balance */
    if (steering == 0.0f) {
        float avg = (s_rpm_pi_left.integral + s_rpm_pi_right.integral) * 0.5f;
        s_rpm_pi_left.integral  = avg;
        s_rpm_pi_right.integral = avg;
    }
    /* Bidirectional RPM closed-loop — keep entire existing block */
    {
        /* ... existing bidirectional logic (lines 950-999) ... */
        float duty_left  = (s_dbg_active && s_dbg_open_loop) ? left_out : s_rpm_duty_left;
        float duty_right = (s_dbg_active && s_dbg_open_loop) ? right_out : s_rpm_duty_right;
        if (failsafe || !armed) {
            duty_left  = 0.0f;
            duty_right = 0.0f;
        }
        left_out  = duty_left;
        right_out = duty_right;
    }
#else
    (void)dt;
    if (failsafe || !armed) {
        left_out  = 0.0f;
        right_out = 0.0f;
    }
#endif
```

**REMOVE the old `#else /* BIBA_OPEN_LOOP ... */` block (lines ~1000-1004).** The #else logic is now in the #else branch above.

**REMOVE the old `#endif /* BIBA_OPEN_LOOP */` at line ~1015.**

**REMOVE the standalone `(void)dt;` at line ~1017** — it's now inside the #else block above.

**B8. Gate LATCH_RECOVERY in tick (currently lines 768-784):**
```c
#if BIBA_FEATURE_LATCH_RECOVERY
    /* ... existing latch reset in tick code (lines 768-784) ... */
#endif
```

**B9. Gate MELODY calls in tick — 7 call sites:**

Wrap each melody call site individually:
- Line ~705 (failsafe melody):
  ```c
  #if BIBA_FEATURE_MELODY
      biba_melody_player_start(&s_player, &biba_melody_failsafe);
  #endif
  ```
- Line ~727 (arm melody): same pattern
- Line ~744 (disarm melody): same pattern
- Line ~863 (trim exit melody): same pattern
- Line ~1055 (control_active stop):
  ```c
  #if BIBA_FEATURE_MELODY
      if (control_active && !s_reverse_pip_active) {
          biba_melody_player_stop(&s_player);
      }
  #endif
  ```
- Line ~1095 (stop on !reverse): same pattern
- Line ~1141 (beacon melody): same pattern
- Lines ~1201-1208 (tick/drive):
  ```c
  #if BIBA_FEATURE_MELODY
      if (s_reverse_pip_active) {
          biba_melody_player_tick_biased(&s_player, now, left_out, right_out);
      } else {
          biba_melody_player_tick(&s_player, now);
      }
  #endif
  ```
- Line ~1212 (motor drive gate):
  When MELODY=0, `s_player.active` is always false so motors are always driven. This logic line:
  ```c
  if (!s_player.active) {
      biba_bts7960_drive(left_out, right_out);
  }
  ```
  MUST REMAIN UNGUARDED — when MELODY=0, s_player.active=false, so motors always drive.

**B10. Rename BIBA_REVERSE_PIP_ENABLED → BIBA_FEATURE_REVERSE_PIP (currently lines ~1080-1104):**

Replace `#if BIBA_REVERSE_PIP_ENABLED` → `#if BIBA_FEATURE_REVERSE_PIP` (and corresponding `#else` / `#endif`).
The existing pattern uses `#if` (value check) — correct, just rename.
Update the `#else` branch to set s_reverse_pip_active=false and s_reverse_pip_next_ms=0u (same as current).

  </action>
  <verify>
    <automated>
# Verify all new guards exist
cd firmware && grep -c '#if BIBA_FEATURE_SPEED_MODE' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_STEERING_DEADBAND' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_HEADING_HOLD' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_MIXER_PROJECTION' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_CURRENT_LIMITER' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_RPM_RAMP' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_LATCH_RECOVERY' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_MELODY' src/modes/mode_standalone.c
cd firmware && grep -c '#if BIBA_FEATURE_REVERSE_PIP' src/modes/mode_standalone.c

# Verify old guards are gone
cd firmware && ! grep -c '#ifndef BIBA_OPEN_LOOP\|#ifdef BIBA_OPEN_LOOP\|BIBA_REVERSE_PIP_ENABLED' src/modes/mode_standalone.c; echo "exit: $?"

# Verify biba_mix_differential is referenced (MIXER_PROJECTION=0 fallback)
cd firmware && grep -c 'biba_mix_differential' src/modes/mode_standalone.c

# Build all-off config (everything off except critical safety)
cd firmware && pio run -e rpico_rp2040_standalone \
  --build-flags="-DBIBA_FEATURE_RPM_CLOSED_LOOP=0 -DBIBA_FEATURE_LATCH_RECOVERY=0 -DBIBA_FEATURE_CURRENT_LIMITER=0 -DBIBA_FEATURE_STEERING_DEADBAND=0 -DBIBA_FEATURE_HEADING_HOLD=0 -DBIBA_FEATURE_SPEED_MODE=0 -DBIBA_FEATURE_MIXER_PROJECTION=0 -DBIBA_FEATURE_MELODY=0" 2>&1 | grep -E "SUCCESS|FAILED|error:"
    </automated>
  </verify>
  <done>
    - Все 17 feature gates внедрены в mode_standalone.c на call sites
    - Старые #ifndef BIBA_OPEN_LOOP / #ifdef BIBA_OPEN_LOOP полностью заменены
    - BIBA_REVERSE_PIP_ENABLED переименован в BIBA_FEATURE_REVERSE_PIP
    - MIXER_PROJECTION=0 использует biba_mix_differential()
    - Default build: SUCCESS
    - All-off build: SUCCESS
    - Каждый отключённый тумблер даёт default-safe поведение (#else: zero floats, pass-through, skip)
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate blackbox record fields + debug telemetry references</name>
  <files>firmware/src/modes/mode_standalone.c</files>
  <action>

**PART A — Blackbox record (lines 1156-1200):**

**A1. Gate rpm_hz10 fields (currently lines 1176-1178):**
Replace:
```c
#ifdef BIBA_OPEN_LOOP
        rec.rpm_left_hz10   = 0;
        rec.rpm_right_hz10  = 0;
#else
        rec.rpm_left_hz10   = (int16_t)(s_pi_meas_hz_left  * 10.0f);
        rec.rpm_right_hz10  = (int16_t)(s_pi_meas_hz_right * 10.0f);
#endif
```
With:
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP
        rec.rpm_left_hz10   = (int16_t)(s_pi_meas_hz_left  * 10.0f);
        rec.rpm_right_hz10  = (int16_t)(s_pi_meas_hz_right * 10.0f);
#else
        rec.rpm_left_hz10   = 0;
        rec.rpm_right_hz10  = 0;
#endif
```

**A2. Gate pi_integral fields (currently ~lines 1195-1196):**
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_PI
        rec.pi_integral_l   = (int16_t)(s_rpm_pi_left.integral  * 10000.0f);
        rec.pi_integral_r   = (int16_t)(s_rpm_pi_right.integral * 10000.0f);
#else
        rec.pi_integral_l   = 0;
        rec.pi_integral_r   = 0;
#endif
```

**A3. Gate pi_meas_ema fields (currently ~lines 1197-1200):**
```c
#if BIBA_FEATURE_RPM_CLOSED_LOOP && BIBA_FEATURE_RPM_ZC
        rec.pi_meas_ema_l   = (uint16_t)(s_telem_meas_ema_left < 0.0f
                                          ? -s_telem_meas_ema_left * 10.0f
                                          :  s_telem_meas_ema_left * 10.0f);
        rec.pi_meas_ema_r   = /* same for right */;
#else
        rec.pi_meas_ema_l   = 0;
        rec.pi_meas_ema_r   = 0;
#endif
```
NOTE: s_telem_meas_ema_left/right are updated in the DR block (which uses zc_ema_update). When ZC=0, these will be 0.0f. The field reference itself won't break because we don't gate variable declarations — only gating the record write avoids writing garbage.

**A4. Gate mean_is fields (currently ~lines 1187-1188):**
```c
#if BIBA_FEATURE_CURRENT_LIMITER || BIBA_FEATURE_RPM_ZC
        rec.mean_is_l       = s_mean_is_left;
        rec.mean_is_r       = s_mean_is_right;
#else
        rec.mean_is_l       = 0;
        rec.mean_is_r       = 0;
#endif
```
NOTE: s_mean_is_left/right are always populated (ADC runs regardless of features). Gating is safety — if current monitoring is off, log zero.

**A5. Gate latch_resets field (currently ~line 1189):**
```c
#if BIBA_FEATURE_LATCH_RECOVERY
        rec.latch_resets    = s_latch_resets;
#else
        rec.latch_resets    = 0;
#endif
```

**PART B — Debug telemetry DRIVE_DATA printf (currently lines 1030-1070):**

The DRIVE_DATA printf is ONLY emitted when `s_dbg_active` is true (debug override via serial). It references many gated variables:
- s_meas_raw_hz_* (always 0.0f when ZC off — safe)
- s_spec_hz_*, s_spec_quality_*, s_spec_peak_*, s_spec_candidate_*, s_spec_reason_* (always 0.0f/0u when DR off — safe)
- s_rpm_pi_left.integral / s_rpm_pi_right.integral (WILL CAUSE COMPILE ERROR if RPM_PI=0 and state variables not gated — but we DON'T gate variable declarations, so struct members exist — SAFE)
- s_freqdet_* fields (always 0 when DR off — safe)

**Decision: Do NOT gate the DRIVE_DATA printf.** Since we don't gate variable declarations (per RESEARCH.md recommendation), all referenced variables exist even when features are off, just with zero values. The debug output will show zeros for disabled features — this is informative, not harmful.

**Verify:** All blackbox fields that reference possibly-disabled features are guarded with `#else → 0`.
  </action>
  <verify>
    <automated>
# Verify blackbox field gates
cd firmware && grep -B2 -A4 'rec.rpm_left_hz10' src/modes/mode_standalone.c | grep -c '#if BIBA_FEATURE_RPM_CLOSED_LOOP'
cd firmware && grep -B2 -A4 'rec.pi_integral_l' src/modes/mode_standalone.c | grep -c '#if BIBA_FEATURE_RPM_CLOSED_LOOP'
cd firmware && grep -B2 -A4 'rec.latch_resets' src/modes/mode_standalone.c | grep -c '#if BIBA_FEATURE_LATCH_RECOVERY'

# Build open-loop (RPM_CLOSED_LOOP=0) — blackbox must compile
cd firmware && pio run -e rpico_rp2040_standalone \
  --build-flags="-DBIBA_FEATURE_RPM_CLOSED_LOOP=0" 2>&1 | grep -E "SUCCESS|FAILED|error:"

# Build with PI=0 — pi_integral fields must compile
cd firmware && pio run -e rpico_rp2040_standalone \
  --build-flags="-DBIBA_FEATURE_RPM_DR=0 -DBIBA_FEATURE_RPM_PI=0" 2>&1 | grep -E "SUCCESS|FAILED|error:"
    </automated>
  </verify>
  <done>
    - Blackbox record: rpm_hz10, pi_integral_*, pi_meas_ema_*, mean_is_*, latch_resets — gated с #else → 0
    - DRIVE_DATA printf не тронута (переменные всегда существуют, значения нулевые при disabled features)
    - Open-loop build (RPM_CLOSED_LOOP=0): SUCCESS
    - PI=0 build: SUCCESS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preprocessor → Compiler | `#if BIBA_FEATURE_<NAME>` resolved at compile time; no runtime codepath divergence possible |
| mode_standalone.c → motor output | When toggles are off, the #else branch provides default-safe values (0.0f duty, pass-through steering, 1.0 speed_scale) — no undefined behavior |

## STRIDE Threat Register (Wave 2 specific)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Denial of Service | LATCH_RECOVERY=0 — BTS7960 thermal latch never auto-cleared | accept | Per RESEARCH.md Risk 2: intentional trade-off for debugging. Hardware SSR interlock still protects. Documented in section header comment. |
| T-12-08 | Denial of Service | CURRENT_LIMITER=0 + MIXER_PROJECTION=0 — BTS7960 overdriven | mitigate | `#warning` at compile time (Plan 01). User must explicitly opt in. Combined with open-loop mode this is maximum performance / minimum safety — expert-only configuration. |
| T-12-09 | Denial of Service | MELODY=0 — s_player.active always false, motors always driven through biba_bts7960_drive | accept | Design intent: when MELODY=0, audio never occupies PWM hardware. Motors always respond to duty commands immediately. |
| T-12-10 | Denial of Service | RPM_PI=0 in tick — left_out/right_out already hold mixer output from earlier in tick | mitigate | Code path: mixer → current limiter (or pass-through) → RPM ramp (or pass-through) → PI duty (or pass-through). When PI=0, the #else branch at B7 does `(void)dt; if (failsafe||!armed) duty=0` — left_out/right_out are unchanged from earlier in the tick, which is the mixer output. Identical to old BIBA_OPEN_LOOP behavior. |

## Equivalence Verification: BIBA_OPEN_LOOP vs BIBA_FEATURE_RPM_CLOSED_LOOP=0

Per RESEARCH.md Section 12.2, all 8 behavioral aspects are preserved:
| Behavior | BIBA_OPEN_LOOP | RPM_CLOSED_LOOP=0 | Match |
|----------|---------------|-------------------|-------|
| ZC skipped | yes | yes (#if fails) | ✓ |
| Spectral skipped | yes | yes | ✓ |
| DR skipped | yes | yes | ✓ |
| PI skipped | yes | yes | ✓ |
| Anti-stall skipped | yes | yes | ✓ |
| RPM ramp skipped | yes | yes (#if && fails) | ✓ |
| s_rpm_duty_* not updated | yes | yes | ✓ |
| left_out/right_out from mixer | yes | yes | ✓ |
| Blackbox rpm_hz = 0 | yes | yes | ✓ |
</threat_model>

<verification>
  <automated>
# Verify old BIBA_OPEN_LOOP references are ALL removed
cd firmware && grep -rn 'BIBA_OPEN_LOOP' src/modes/mode_standalone.c && echo "FAIL: old guards remain" || echo "PASS: all old guards removed"

# Verify old BIBA_REVERSE_PIP_ENABLED renamed
cd firmware && grep -rn 'BIBA_REVERSE_PIP_ENABLED' src/modes/mode_standalone.c && echo "FAIL: old name remains" || echo "PASS: renamed"

# Count total BIBA_FEATURE_ occurrences in mode_standalone.c
cd firmware && echo "Total FEATURE gates:" && grep -c '#if BIBA_FEATURE_' src/modes/mode_standalone.c

# Build default + open-loop + all-off
cd firmware && pio run -e rpico_rp2040_standalone 2>&1 | grep -E "SUCCESS|FAILED"
cd firmware && pio run -e rpico_rp2040_standalone --build-flags="-DBIBA_FEATURE_RPM_CLOSED_LOOP=0" 2>&1 | grep -E "SUCCESS|FAILED"
cd firmware && pio run -e rpico_rp2040_standalone --build-flags="-DBIBA_FEATURE_RPM_CLOSED_LOOP=0 -DBIBA_FEATURE_LATCH_RECOVERY=0 -DBIBA_FEATURE_CURRENT_LIMITER=0 -DBIBA_FEATURE_STEERING_DEADBAND=0 -DBIBA_FEATURE_HEADING_HOLD=0 -DBIBA_FEATURE_SPEED_MODE=0 -DBIBA_FEATURE_MIXER_PROJECTION=0 -DBIBA_FEATURE_MELODY=0" 2>&1 | grep -E "SUCCESS|FAILED"
  </automated>
</verification>

<success_criteria>
- [ ] Все 17 feature gates внедрены в mode_standalone.c на call sites (ISR + init + tick + blackbox)
- [ ] Старые #ifndef BIBA_OPEN_LOOP / #ifdef BIBA_OPEN_LOOP полностью удалены (FEAT-02)
- [ ] BIBA_REVERSE_PIP_ENABLED переименован в BIBA_FEATURE_REVERSE_PIP (D-03)
- [ ] biba_mix_differential() используется при MIXER_PROJECTION=0
- [ ] Blackbox поля gated: rpm_hz10, pi_integral_*, pi_meas_ema_*, mean_is_*, latch_resets
- [ ] Default build: SUCCESS
- [ ] Open-loop build (RPM_CLOSED_LOOP=0): SUCCESS
- [ ] All-off build: SUCCESS
- [ ] Поведение при RPM_CLOSED_LOOP=0 идентично старому BIBA_OPEN_LOOP
</success_criteria>

<output>
After completion, create `.planning/phases/12-feature-gating/12-02-SUMMARY.md`
</output>
