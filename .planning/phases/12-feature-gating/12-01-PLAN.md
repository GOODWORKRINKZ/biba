---
phase: 12-feature-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/include/biba_config.h
  - firmware/targets/RPICO_RP2040/target_config.h
  - firmware/src/app/zc_detector.h
  - firmware/src/app/rpm_spectral_estimator.h
  - firmware/src/app/rpm_pi.h
autonomous: true
requirements:
  - FEAT-01
  - FEAT-03
  - FEAT-04

must_haves:
  truths:
    - "Каждая из 17 фич имеет BIBA_FEATURE_<NAME> тумблер в biba_config.h, default=1 (кроме REVERSE_PIP=0)"
    - "biba_config.h содержит секции-комментарии вида /* --- Feature: ... */ с тумблером и параметрами вместе"
    - "Нарушение зависимостей PI→DR, DUAL_WINDOW→SPECTRAL, LOAD_GATE→SPECTRAL, ANTI_STALL→SPECTRAL ловится #error"
    - "Старый BIBA_OPEN_LOOP мапится в BIBA_FEATURE_RPM_CLOSED_LOOP=0 с #warning"
    - "BIBA_REVERSE_PIP_ENABLED переименован в BIBA_FEATURE_REVERSE_PIP"
  artifacts:
    - path: "firmware/include/biba_config.h"
      provides: "Все 17 тумблеров + секции + dependency #error + backward compat"
      contains: "BIBA_FEATURE_RPM_CLOSED_LOOP"
      contains: "BIBA_FEATURE_RPM_ZC"
      contains: "BIBA_FEATURE_RPM_SPECTRAL"
      contains: "BIBA_FEATURE_RPM_DUAL_WINDOW"
      contains: "BIBA_FEATURE_RPM_LOAD_GATE"
      contains: "BIBA_FEATURE_RPM_DR"
      contains: "BIBA_FEATURE_RPM_PI"
      contains: "BIBA_FEATURE_RPM_ANTI_STALL"
      contains: "BIBA_FEATURE_LATCH_RECOVERY"
      contains: "BIBA_FEATURE_CURRENT_LIMITER"
      contains: "BIBA_FEATURE_STEERING_DEADBAND"
      contains: "BIBA_FEATURE_RPM_RAMP"
      contains: "BIBA_FEATURE_MELODY"
      contains: "BIBA_FEATURE_REVERSE_PIP"
      contains: "BIBA_FEATURE_HEADING_HOLD"
      contains: "BIBA_FEATURE_SPEED_MODE"
      contains: "BIBA_FEATURE_MIXER_PROJECTION"
      contains: "#error"
      contains: "#warning"
      contains: "BIBA_OPEN_LOOP"
    - path: "firmware/targets/RPICO_RP2040/target_config.h"
      provides: "RP2040 target overrides: REVERSE_PIP=0 (per D-07)"
    - path: "firmware/src/app/zc_detector.h"
      provides: "ZC params guarded by #ifndef (biba_config.h defaults win)"
    - path: "firmware/src/app/rpm_spectral_estimator.h"
      provides: "Spectral params guarded by #ifndef (biba_config.h defaults win)"
    - path: "firmware/src/app/rpm_pi.h"
      provides: "PI params guarded by #ifndef (biba_config.h defaults win)"
  key_links:
    - from: "biba_config.h section FEATURE_RPM_CLOSED_LOOP"
      to: "biba_config.h #error checks at bottom"
      via: "Master switch gates dependency checks"
      pattern: "#if BIBA_FEATURE_RPM_CLOSED_LOOP"
    - from: "biba_config.h #ifdef BIBA_OPEN_LOOP"
      to: "biba_config.h #define BIBA_FEATURE_RPM_CLOSED_LOOP 0"
      via: "Backward compat deprecation path"
      pattern: "#ifdef BIBA_OPEN_LOOP"
    - from: "target_config.h"
      to: "biba_config.h #ifndef defaults"
      via: "#include \"target_config.h\" before defaults"
      pattern: "target_config.h.*#define.*0"
---

<objective>
Реорганизовать `biba_config.h` — ввести все 17 тумблеров `BIBA_FEATURE_*` с секциями-комментариями, перенести параметры фич из модульных `.h`, добавить dependency `#error` проверки и backward-compat `BIBA_OPEN_LOOP` → `BIBA_FEATURE_RPM_CLOSED_LOOP`.

Purpose: Создать единый источник правды для всех feature toggles, где каждая фича со своими параметрами сгруппирована в именованной секции, а нарушения зависимостей ловятся на этапе компиляции.
Output: Полностью реорганизованный `biba_config.h` (~500 строк), обновлённые `zc_detector.h`, `rpm_spectral_estimator.h`, `rpm_pi.h` с `#ifndef`-гардами, минимальное изменение `target_config.h`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-feature-gating/12-CONTEXT.md
@.planning/phases/12-feature-gating/12-RESEARCH.md

<interfaces>
<!-- Key structure of current biba_config.h for executor reference -->

Current biba_config.h structure (3 sections, ~310 lines):
  1. Lines 1-18: Include guard + preamble
  2. Lines 20-35: BIBA_NATIVE_TEST fallback block
  3. Lines 37-310: Flat #ifndef/#define defaults (control loop, motor/PWM, CRSF, SPI, channels, arm, speed mode, drive mode, trim, deadband, steering deadband, reverse pip, motor direction, motor ramp, RPM setpoint ramp, calibration, blackbox, DR params, load gate params)
  4. Lines 312-319: BIBA_OPEN_LOOP unconditional define + closing #endif

Include order in mode_standalone.c line 14: #include "biba_config.h" comes BEFORE all module includes.
This means moving constants to biba_config.h with #ifndef guards is safe — they'll be defined before modules read them.

Target override order: target_config.h is #included at biba_config.h line 21 (inside BIBA_NATIVE_TEST guard).
Target_config.h uses direct #define (no #ifndef), so its values ALWAYS win over #ifndef-guarded defaults.

Key constants to MOVE from module .h files to biba_config.h feature sections:
  FROM zc_detector.h: ZC_SUBWIN_K, ZC_SUBWIN_MIN_PKPK, ZC_SUBWIN_MIN_STD, ZC_MIN_VALID_HZ, ZC_EMA_ALPHA
  FROM rpm_spectral_estimator.h: BIBA_RPM_SPECTRAL_MIN_TARGET_HZ, MAX_TARGET_HZ, REL_BAND, ABS_BAND_HZ, MIN_PEAK_AMP_LSB, MIN_QUALITY
  FROM rpm_pi.h: BIBA_RPM_PI_KP, KI, KI_LOW, KI_LOW_THRESH, FF_SLOPE, FF_DEAD, STICTION, P_CLAMP, DT_S

Existing header ordering constants in biba_config.h that stay in non-feature sections:
  - Control loop timing (BIBA_CONTROL_LOOP_HZ, BIBA_TELEMETRY_PUBLISH_HZ)
  - Motor/PWM (BIBA_PWM_FREQUENCY_HZ, BIBA_PWM_DEADTIME_NS, BIBA_BTS7960_RESET_PULSE_US)
  - CRSF link (BIBA_CRSF_BAUD, BIBA_CRSF_TIMEOUT_MS)
  - RC channels (BIBA_CH_THROTTLE, STEERING, ARM, SPEED_MODE, DRIVE_MODE, BEACON, TRIM, BLACKBOX)
  - Arm threshold (BIBA_ARM_THRESHOLD)
  - Motor deadband (BIBA_MOTOR_DEADBAND)
  - Motor direction (BIBA_LEFT_MOTOR_DIR, BIBA_RIGHT_MOTOR_DIR)
  - System/SPI (BIBA_SYS_CLOCK_HZ, BIBA_SPI_LINK_TIMEOUT_MS)
  - Current sense (BIBA_IS_ZERO_OFFSET_V, BIBA_IS_AMPS_PER_VOLT)
  - Battery (BIBA_VBAT_DIVIDER_RATIO, BIBA_IBAT_AMPS_PER_VOLT, BIBA_IBAT_ZERO_OFFSET_V, BIBA_RAIL_12V_DIVIDER_RATIO, BIBA_ADC_VREF_V, BIBA_ADC_MAX_COUNTS)
  - Motor trim (BIBA_MOTOR_TRIM_MAX_EFFECT, BIBA_TRIM_GESTURE_THRESHOLD, BIBA_TRIM_CONFIRM_HOLD_MS)
  - Motor ramp (BIBA_RAMP_ACCEL_RATE, DECEL_RATE, REVERSE_DECEL_RATE, ZERO_HOLD_MS)
  - RPM calibration (BIBA_RPM_GEAR_RATIO, COMMUTATOR_BARS, PULSES_PER_WHEEL_REV, RPM_MAX_HZ)
  - Blackbox (BIBA_BLACKBOX_RATE_HZ, FIELD_MASK, MIN_FREE_KB, BIBA_CH_BLACKBOX)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize biba_config.h — non-feature sections + 17 feature toggles</name>
  <files>firmware/include/biba_config.h</files>
  <action>

**STEP 1: Non-feature config sections (lines 37-310 of current file → restructured)**

Reorganize the existing flat #ifndef/#define list into labeled sections. These constants DO NOT get toggles per D-05:

1. `/* --- Control loop timing --- */` — BIBA_CONTROL_LOOP_HZ, BIBA_TELEMETRY_PUBLISH_HZ
2. `/* --- Motor / PWM --- */` — BIBA_PWM_FREQUENCY_HZ, BIBA_PWM_DEADTIME_NS, BIBA_BTS7960_RESET_PULSE_US
3. `/* --- CRSF link --- */` — BIBA_CRSF_BAUD, BIBA_CRSF_TIMEOUT_MS
4. `/* --- RC channel assignments --- */` — BIBA_CH_THROTTLE, STEERING, ARM, SPEED_MODE, DRIVE_MODE, BEACON, TRIM, BLACKBOX
5. `/* --- Arm threshold --- */` — BIBA_ARM_THRESHOLD
6. `/* --- Motor deadband --- */` — BIBA_MOTOR_DEADBAND
7. `/* --- Motor direction --- */` — BIBA_LEFT_MOTOR_DIR, BIBA_RIGHT_MOTOR_DIR
8. `/* --- System / SPI --- */` — BIBA_SYS_CLOCK_HZ, BIBA_SPI_LINK_TIMEOUT_MS

Preserve existing #ifndef guards for each constant — do NOT change any values. Only re-group with comment headers.

**STEP 2: Feature toggle sections (NEW — 17 sections, each with pattern per D-04)**

For EACH toggle, create a section using the D-04 template:

```c
/* --- Feature: [Human-readable name] ------------------------------------ */
#ifndef BIBA_FEATURE_<NAME>
#  define BIBA_FEATURE_<NAME>            1  /* 1=enabled, 0=disabled */
#endif
/* [param1 description] */
#ifndef BIBA_<PARAM1>
#  define BIBA_<PARAM1>                  <value>
#endif
/* ... repeat for each param ... */
```

Default values per D-07: ALL toggles = 1 except REVERSE_PIP = 0.

**Section ordering (per RESEARCH.md Section 5.1):**

9. `/* --- Feature: RPM Closed-Loop Master --- */` — BIBA_FEATURE_RPM_CLOSED_LOOP. This toggle has NO params — it's a pure gate.
10. `/* --- Feature: RPM ZC Detector --- */` — BIBA_FEATURE_RPM_ZC + params MOVED from zc_detector.h: ZC_SUBWIN_K (default from zc_detector.h), ZC_SUBWIN_MIN_PKPK, ZC_SUBWIN_MIN_STD, ZC_MIN_VALID_HZ, ZC_EMA_ALPHA. Use EXISTING default values from zc_detector.h — do not invent new ones.
11. `/* --- Feature: RPM Spectral Estimator --- */` — BIBA_FEATURE_RPM_SPECTRAL + params MOVED from rpm_spectral_estimator.h: BIBA_RPM_SPECTRAL_MIN_TARGET_HZ, BIBA_RPM_SPECTRAL_MAX_TARGET_HZ, BIBA_RPM_SPECTRAL_REL_BAND, BIBA_RPM_SPECTRAL_ABS_BAND_HZ, BIBA_RPM_SPECTRAL_MIN_PEAK_AMP_LSB, BIBA_RPM_SPECTRAL_MIN_QUALITY. Use EXISTING values.
12. `/* --- Feature: RPM Dual-Window Search --- */` — BIBA_FEATURE_RPM_DUAL_WINDOW. No params yet — placeholder section for future dual-window config params.
13. `/* --- Feature: RPM IS-Pin Load Gate --- */` — BIBA_FEATURE_RPM_LOAD_GATE + existing params: BIBA_RPM_LOAD_RATIO_THRESH, BIBA_RPM_LOAD_QUALITY_MAX, BIBA_RPM_LOAD_ABS_THRESH_ADC (keep current values from lines 293-310).
14. `/* --- Feature: RPM Dead Reckoning --- */` — BIBA_FEATURE_RPM_DR + existing params: BIBA_RPM_DR_MAX_STREAK, BIBA_RPM_DR_RATIO_LO, BIBA_RPM_DR_RATIO_HI, BIBA_RPM_DR_ALPHA (keep current values from lines 279-291).
15. `/* --- Feature: RPM PI Controller --- */` — BIBA_FEATURE_RPM_PI + params MOVED from rpm_pi.h: BIBA_RPM_PI_KP, BIBA_RPM_PI_KI, BIBA_RPM_PI_KI_LOW, BIBA_RPM_PI_KI_LOW_THRESH, BIBA_RPM_PI_FF_SLOPE, BIBA_RPM_PI_FF_DEAD, BIBA_RPM_PI_STICTION, BIBA_RPM_PI_P_CLAMP, BIBA_RPM_PI_DT_S. Use EXISTING values from rpm_pi.h.
16. `/* --- Feature: RPM Anti-Stall --- */` — BIBA_FEATURE_RPM_ANTI_STALL + local constants (defined in mode_standalone.c lines 213-215, move HERE as BIBA_ANTISTALL_RAMP_STEP, BIBA_ANTISTALL_MAX_DUTY, BIBA_ANTISTALL_CONFIRM — use existing values: 0.02f, 0.90f, 3u respectively per RESEARCH.md refs).
17. `/* --- RPM Calibration --- */` — NOT a toggle. Keep existing: BIBA_RPM_GEAR_RATIO, BIBA_RPM_COMMUTATOR_BARS, BIBA_RPM_PULSES_PER_WHEEL_REV, BIBA_RPM_MAX_HZ.
18. `/* --- Feature: Latch Recovery --- */` — BIBA_FEATURE_LATCH_RECOVERY + params (move from mode_standalone.c lines 200-203): BIBA_LATCH_IS_RAW_MIN, BIBA_LATCH_BLOCKS_CONFIRM, BIBA_LATCH_COOLDOWN_WINDOWS. Use existing values: 3500, 3u, 20u.
19. `/* --- Feature: Current Limiter --- */` — BIBA_FEATURE_CURRENT_LIMITER + existing params: BIBA_LEFT_MAX_CURRENT_A, BIBA_RIGHT_MAX_CURRENT_A, BIBA_LEFT_MAX_POWER_W, BIBA_RIGHT_MAX_POWER_W, BIBA_FALLBACK_SUPPLY_V. Keep current values from lines 58-71.
20. `/* --- Feature: Steering Deadband --- */` — BIBA_FEATURE_STEERING_DEADBAND + BIBA_STEERING_DEADBAND (keep current value 0.20f).
21. `/* --- Feature: RPM Setpoint Ramp --- */` — BIBA_FEATURE_RPM_RAMP + existing params: BIBA_RPM_SETPOINT_ACCEL_RATE, BIBA_RPM_SETPOINT_DECEL_RATE, BIBA_RPM_SETPOINT_REVERSE_DECEL_RATE, BIBA_RPM_SETPOINT_ZERO_HOLD_MS (keep current values from lines 244-255).
22. `/* --- Feature: Motor Coil Melody --- */` — BIBA_FEATURE_MELODY. No params beyond toggle (melody data lives in melody.c).
23. `/* --- Feature: Reverse Backup Pip --- */` — BIBA_FEATURE_REVERSE_PIP (was BIBA_REVERSE_PIP_ENABLED) + BIBA_REVERSE_PIP_INTERVAL_MS. DEFAULT = 0 per D-07.
24. `/* --- Feature: Heading Hold --- */` — BIBA_FEATURE_HEADING_HOLD + PID gains: BIBA_HEADING_KP, BIBA_HEADING_KI, BIBA_HEADING_KD, BIBA_HEADING_OUTPUT_LIMIT, BIBA_HEADING_INTEGRAL_LIMIT. READ existing values from mode_standalone.c lines 85-88 (s_heading_cfg). Use EXACT values — do NOT guess.
25. `/* --- Feature: Speed Mode --- */` — BIBA_FEATURE_SPEED_MODE + existing params: BIBA_SPEED_MODE_LOW_THRESHOLD, BIBA_SPEED_MODE_HIGH_THRESHOLD, BIBA_SPEED_MODE_SLOW_SCALE, BIBA_SPEED_MODE_MEDIUM_SCALE, BIBA_SPEED_MODE_FAST_SCALE. Keep current values.
26. `/* --- Feature: Mixer Projection --- */` — BIBA_FEATURE_MIXER_PROJECTION. No params (L∞ ball is pure algorithm).

**STEP 3: Non-feature config (continued after toggles)**

27. `/* --- Current sense calibration --- */` — BIBA_IS_ZERO_OFFSET_V, BIBA_IS_AMPS_PER_VOLT
28. `/* --- Battery --- */` — BIBA_VBAT_DIVIDER_RATIO, BIBA_IBAT_AMPS_PER_VOLT, BIBA_IBAT_ZERO_OFFSET_V, BIBA_RAIL_12V_DIVIDER_RATIO, BIBA_ADC_VREF_V, BIBA_ADC_MAX_COUNTS
29. `/* --- Motor trim --- */` — BIBA_MOTOR_TRIM_MAX_EFFECT, BIBA_TRIM_GESTURE_THRESHOLD, BIBA_TRIM_CONFIRM_HOLD_MS
30. `/* --- Motor output ramp --- */` — BIBA_RAMP_ACCEL_RATE, BIBA_RAMP_DECEL_RATE, BIBA_RAMP_REVERSE_DECEL_RATE, BIBA_RAMP_ZERO_HOLD_MS
31. `/* --- Blackbox --- */` — BIBA_BLACKBOX_RATE_HZ, BIBA_BLACKBOX_FIELD_MASK, BIBA_BLACKBOX_MIN_FREE_KB, BIBA_CH_BLACKBOX

**STEP 4: Dependency #error checks (at VERY END, after ALL above sections)**

Per D-06, add after all toggles defined, BEFORE closing #endif:

```c
/* --- Dependency validation ---------------------------------------------- */
#if BIBA_FEATURE_RPM_CLOSED_LOOP
#  if BIBA_FEATURE_RPM_PI && !BIBA_FEATURE_RPM_DR
#    error "BIBA_FEATURE_RPM_PI requires BIBA_FEATURE_RPM_DR (PI uses DR as measurement source)"
#  endif
#  if BIBA_FEATURE_RPM_DUAL_WINDOW && !BIBA_FEATURE_RPM_SPECTRAL
#    error "BIBA_FEATURE_RPM_DUAL_WINDOW requires BIBA_FEATURE_RPM_SPECTRAL (hint is a second Goertzel search)"
#  endif
#  if BIBA_FEATURE_RPM_LOAD_GATE && !BIBA_FEATURE_RPM_SPECTRAL
#    error "BIBA_FEATURE_RPM_LOAD_GATE requires BIBA_FEATURE_RPM_SPECTRAL (gate applied to spectral result)"
#  endif
#  if BIBA_FEATURE_RPM_ANTI_STALL && !BIBA_FEATURE_RPM_SPECTRAL
#    error "BIBA_FEATURE_RPM_ANTI_STALL requires BIBA_FEATURE_RPM_SPECTRAL (uses HIGH_LOAD from spectral)"
#  endif
#endif /* BIBA_FEATURE_RPM_CLOSED_LOOP */
```

**STEP 5: Backward compat — BIBA_OPEN_LOOP deprecation**

Place IMMEDIATELY BEFORE the RPM_CLOSED_LOOP section (Step 2, item 9), NOT at the very end:

```c
/* Backward compatibility: old BIBA_OPEN_LOOP maps to master switch. */
#ifdef BIBA_OPEN_LOOP
#  warning "BIBA_OPEN_LOOP is deprecated — use BIBA_FEATURE_RPM_CLOSED_LOOP=0"
#  ifndef BIBA_FEATURE_RPM_CLOSED_LOOP
#    define BIBA_FEATURE_RPM_CLOSED_LOOP 0
#  endif
#endif
```

**STEP 6: REMOVE the unconditional `#define BIBA_OPEN_LOOP` (current lines 318-319).**

This is critical — the old define LINE MUST BE DELETED. The backward compat block above replaces it.

**STEP 7: Add `#warning` for CURRENT_LIMITER=0 (Risk 3 from RESEARCH.md Section 13)**

After the CURRENT_LIMITER section, add:
```c
#if !BIBA_FEATURE_CURRENT_LIMITER
#  warning "Current limiter disabled — BTS7960 overcurrent protection is OFF (expert mode)"
#endif
```

**CRITICAL CONSTRAINTS:**
- All `#ifndef` guards must be BEFORE `#error` checks (include-order: target_config.h → defaults → #error)
- ALL toggle defines use `#if BIBA_FEATURE_<NAME>` (value check), NEVER `#ifdef BIBA_FEATURE_<NAME>`
- The `#include "target_config.h"` at line 21 STAYS — do NOT move it
- The `BIBA_NATIVE_TEST` block (lines 20-35) STAYS — add toggle defaults OUTSIDE this block, after it
- Keep ALL existing constant VALUES unchanged — this task is reorganization, NOT re-tuning

**CONSTANT MIGRATION NOTES (exact values must be read from source):**
Before copying constants from module .h files, READ the actual numeric values from:
- `firmware/src/app/zc_detector.h` (ZC params)
- `firmware/src/app/rpm_spectral_estimator.h` (spectral params)
- `firmware/src/app/rpm_pi.h` (PI params)
- `firmware/src/modes/mode_standalone.c` lines 85-88 (heading PID gains)
- `firmware/src/modes/mode_standalone.c` lines 200-203 (latch params)
- `firmware/src/modes/mode_standalone.c` lines 213-215 (antistall params)

DO NOT GUESS values — read them from source.
  </action>
  <verify>
    <automated>grep -c 'BIBA_FEATURE_' firmware/include/biba_config.h | grep -v '^#' | tail -1</automated>
    <expect>At least 34 occurrences (17 toggles × 2: #ifndef guard + default #define).</expect>
  </verify>
  <done>
    - biba_config.h содержит 17 секций BIBA_FEATURE_* с тумблерами (default=1 кроме REVERSE_PIP=0)
    - Все параметры фич сгруппированы внутри своих секций
    - 4 dependency #error проверки в конце файла
    - BIBA_OPEN_LOOP backward compat с #warning
    - Старый `#define BIBA_OPEN_LOOP` удалён
    - CURRENT_LIMITER=0 выдаёт #warning
    - Файл структурно чистый: секции-комментарии, группировка toggle+params, #error checks
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard module .h parameter defaults with #ifndef</name>
  <files>
    firmware/src/app/zc_detector.h,
    firmware/src/app/rpm_spectral_estimator.h,
    firmware/src/app/rpm_pi.h
  </files>
  <action>

For each module header, wrap existing #define defaults with #ifndef guards so `biba_config.h` wins when included first.

**zc_detector.h:**
- Find: `#define ZC_SUBWIN_K`, `#define ZC_SUBWIN_MIN_PKPK`, `#define ZC_SUBWIN_MIN_STD`, `#define ZC_MIN_VALID_HZ`, `#define ZC_EMA_ALPHA`
- Wrap each: `#ifndef ZC_SUBWIN_K` / `#  define ZC_SUBWIN_K <existing_value>` / `#endif`
- Add comment: `/* Default moved to biba_config.h; kept for backward compat */` above the block

**rpm_spectral_estimator.h:**
- Find: `#define BIBA_RPM_SPECTRAL_MIN_TARGET_HZ`, `MAX_TARGET_HZ`, `REL_BAND`, `ABS_BAND_HZ`, `MIN_PEAK_AMP_LSB`, `MIN_QUALITY`
- Wrap each with #ifndef guard
- Add backward compat comment

**rpm_pi.h:**
- Find: `#define BIBA_RPM_PI_KP`, `KI`, `KI_LOW`, `KI_LOW_THRESH`, `FF_SLOPE`, `FF_DEAD`, `STICTION`, `P_CLAMP`, `DT_S`
- Wrap each with #ifndef guard
- Add backward compat comment

**CRITICAL:** Do NOT change any values. Only add `#ifndef` guard around each existing `#define`.

This ensures: if target overrides are set in biba_config.h before these headers are included, the biba_config.h values win. If biba_config.h is NOT included (e.g., unit tests that include headers directly), the module defaults still work.
  </action>
  <verify>
    <automated>cd firmware && grep -c '#ifndef ZC_SUBWIN_K' src/app/zc_detector.h && grep -c '#ifndef BIBA_RPM_SPECTRAL_MIN_TARGET_HZ' src/app/rpm_spectral_estimator.h && grep -c '#ifndef BIBA_RPM_PI_KP' src/app/rpm_pi.h</automated>
    <expect>Each returns 1 (exactly one #ifndef guard per param group).</expect>
  </verify>
  <done>
    - zc_detector.h, rpm_spectral_estimator.h, rpm_pi.h — все параметры защищены #ifndef
    - Значения не изменены
    - Комментарии о backward compat присутствуют
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verification — default build + open-loop build + dependency #error tests</name>
  <files>firmware/include/biba_config.h</files>
  <action>

Verify the biba_config.h reorganization compiles correctly in all critical configurations:

**Test 1 — Default build (all toggles=1):**
```bash
cd firmware && pio run -e rpico_rp2040_standalone 2>&1 | tail -20
```
Expected: SUCCESS. No errors, no warnings (except the expected CURRENT_LIMITER one if limits are 0.0f in target_config.h — this is OK, the target sets 0.0f which is intentional for RP2040 before HW rework).

**Test 2 — Open-loop build (master off):**
```bash
cd firmware && pio run -e rpico_rp2040_standalone \
  --build-flags="-DBIBA_FEATURE_RPM_CLOSED_LOOP=0" 2>&1 | tail -20
```
Expected: SUCCESS. No errors. Should see backward compat #warning if BIBA_OPEN_LOOP is still defined anywhere, or no warning if it was removed.

**Test 3 — Dependency violation test (PI=1 but DR=0):**
```bash
cd firmware && pio run -e rpico_rp2040_standalone \
  --build-flags="-DBIBA_FEATURE_RPM_DR=0" 2>&1 | grep -E "error|Error"
```
Expected: `#error "BIBA_FEATURE_RPM_PI requires BIBA_FEATURE_RPM_DR..."` — BUILD FAILS with clear message.

**Test 3b — Verify DR=0 but PI=0 compiles OK:**
```bash
cd firmware && pio run -e rpico_rp2040_standalone \
  --build-flags="-DBIBA_FEATURE_RPM_DR=0 -DBIBA_FEATURE_RPM_PI=0" 2>&1 | tail -5
```
Expected: SUCCESS.

**Test 3c — DUAL_WINDOW=1, SPECTRAL=0:**
```bash
cd firmware && pio run -e rpico_rp2040_standalone \
  --build-flags="-DBIBA_FEATURE_RPM_SPECTRAL=0" 2>&1 | grep -E "error|Error"
```
Expected: `#error "BIBA_FEATURE_RPM_DUAL_WINDOW requires BIBA_FEATURE_RPM_SPECTRAL..."` — BUILD FAILS.

**Test 4 — All features off except critical safety:**
```bash
cd firmware && pio run -e rpico_rp2040_standalone \
  --build-flags="-DBIBA_FEATURE_RPM_CLOSED_LOOP=0 -DBIBA_FEATURE_LATCH_RECOVERY=0 -DBIBA_FEATURE_CURRENT_LIMITER=0 -DBIBA_FEATURE_STEERING_DEADBAND=0 -DBIBA_FEATURE_HEADING_HOLD=0 -DBIBA_FEATURE_SPEED_MODE=0 -DBIBA_FEATURE_MIXER_PROJECTION=0 -DBIBA_FEATURE_MELODY=0" 2>&1 | tail -10
```
Expected: SUCCESS with CURRENT_LIMITER=0 #warning. This verifies the chain compiles with everything off.

**Test 5 — REVERSE_PIP=0 (RP2040 default) compile check:**
```bash
cd firmware && pio run -e rpico_rp2040_standalone 2>&1 | grep -i "reverse_pip\|REVERSE_PIP"
```
Expected: No error lines. REVERSE_PIP=0 should compile cleanly (this matches the current default in target_config.h — actually CURRENT code has `BIBA_REVERSE_PIP_ENABLED 0`).

If any build fails for reasons OTHER than expected #error triggers, fix the issue in biba_config.h before proceeding.
  </action>
  <verify>
    <automated>
# Run all 5 build tests sequentially
cd firmware && \
  echo "=== Test 1: Default ===" && pio run -e rpico_rp2040_standalone 2>&1 | grep -E "SUCCESS|FAILED|error:" && \
  echo "=== Test 2: Open-loop ===" && pio run -e rpico_rp2040_standalone --build-flags="-DBIBA_FEATURE_RPM_CLOSED_LOOP=0" 2>&1 | grep -E "SUCCESS|FAILED|error:" && \
  echo "=== Test 3: DR=0 should FAIL ===" && pio run -e rpico_rp2040_standalone --build-flags="-DBIBA_FEATURE_RPM_DR=0" 2>&1 | grep -E "BIBA_FEATURE_RPM_PI requires|error:" && \
  echo "=== Test 4: DR=0 PI=0 should SUCCEED ===" && pio run -e rpico_rp2040_standalone --build-flags="-DBIBA_FEATURE_RPM_DR=0 -DBIBA_FEATURE_RPM_PI=0" 2>&1 | grep -E "SUCCESS|FAILED|error:" && \
  echo "=== Test 5: All off ===" && pio run -e rpico_rp2040_standalone --build-flags="-DBIBA_FEATURE_RPM_CLOSED_LOOP=0 -DBIBA_FEATURE_LATCH_RECOVERY=0 -DBIBA_FEATURE_CURRENT_LIMITER=0 -DBIBA_FEATURE_STEERING_DEADBAND=0 -DBIBA_FEATURE_HEADING_HOLD=0 -DBIBA_FEATURE_SPEED_MODE=0 -DBIBA_FEATURE_MIXER_PROJECTION=0 -DBIBA_FEATURE_MELODY=0" 2>&1 | grep -E "SUCCESS|FAILED|error:"
    </automated>
  </verify>
  <done>
    - Default build (all toggles=1): SUCCESS
    - Open-loop build (RPM_CLOSED_LOOP=0): SUCCESS
    - Dependency violation (PI=1, DR=0): BUILD FAILS with clear #error message
    - Corrected dependency (PI=0, DR=0): SUCCESS
    - All-off build (8 toggles=0): SUCCESS with expected CURRENT_LIMITER #warning
    - REVERSE_PIP=0: clean compile
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| biba_config.h → mode_standalone.c | Toggle values compiled into firmware image; no runtime mutation boundary |
| target_config.h → biba_config.h | Target override values cross into default namespace via #ifndef guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | biba_config.h feature toggle defaults | mitigate | `#ifndef` guards prevent accidental override; `#error` catches invalid combos; ONLY way to change toggle is editing target_config.h and recompiling |
| T-12-02 | Denial of Service | RPM chain when sub-toggle off but master on | mitigate | Dependency `#error` checks (D-06) prevent known-broken combos (PI→DR, DUAL_WINDOW→SPECTRAL, LOAD_GATE→SPECTRAL, ANTI_STALL→SPECTRAL). Non-enforced combos (ZC off, SPECTRAL on) produce telemetry zeros — documented as "expert mode, no guard" |
| T-12-03 | Elevation of Privilege | CURRENT_LIMITER=0 removing overcurrent protection | mitigate | `#warning` emitted at compile time; documented as "expert mode only" in section header comment |
| T-12-04 | Denial of Service | Include-order bug — #error sees defaults instead of target overrides | mitigate | `#include "target_config.h"` (line 21) is BEFORE all `#ifndef` defaults AND before `#error` checks. Verified in RESEARCH.md Section 13 Risk 6. |
| T-12-05 | Information Disclosure | Debug telemetry referencing gated variables | accept | Variables not gated in Wave 1 (only call sites). Telemetry reads zeroed values when feature off — no info leak, safe default. Full variable gating deferred per RESEARCH.md recommendation. |
| T-12-06 | Tampering | BIBA_OPEN_LOOP accidentally re-enabled by stale target_config.h | mitigate | Backward compat `#ifdef BIBA_OPEN_LOOP` with `#warning` clearly signals deprecation. The warning is visible in every build that uses the old flag. |

## ASVS L1 Controls Applicable

| ASVS Requirement | Control Applied |
|------------------|-----------------|
| V5.1.1 Input validation | CRSF frame validation unchanged; not affected by toggles |
| V5.5.4 Untrusted data in build | Toggles are compile-time constants from versioned source files; no runtime injection vector |
</threat_model>

<verification>
  <automated>
# Verify biba_config.h has all 17 toggle defines
cd firmware && grep -c 'BIBA_FEATURE_RPM_CLOSED_LOOP\|BIBA_FEATURE_RPM_ZC\|BIBA_FEATURE_RPM_SPECTRAL\|BIBA_FEATURE_RPM_DUAL_WINDOW\|BIBA_FEATURE_RPM_LOAD_GATE\|BIBA_FEATURE_RPM_DR\|BIBA_FEATURE_RPM_PI\|BIBA_FEATURE_RPM_ANTI_STALL\|BIBA_FEATURE_LATCH_RECOVERY\|BIBA_FEATURE_CURRENT_LIMITER\|BIBA_FEATURE_STEERING_DEADBAND\|BIBA_FEATURE_RPM_RAMP\|BIBA_FEATURE_MELODY\|BIBA_FEATURE_REVERSE_PIP\|BIBA_FEATURE_HEADING_HOLD\|BIBA_FEATURE_SPEED_MODE\|BIBA_FEATURE_MIXER_PROJECTION' include/biba_config.h

# Verify dependency #error checks exist
cd firmware && grep -c '#error.*BIBA_FEATURE_RPM_PI requires' include/biba_config.h
cd firmware && grep -c '#error.*BIBA_FEATURE_RPM_DUAL_WINDOW requires' include/biba_config.h
cd firmware && grep -c '#error.*BIBA_FEATURE_RPM_LOAD_GATE requires' include/biba_config.h
cd firmware && grep -c '#error.*BIBA_FEATURE_RPM_ANTI_STALL requires' include/biba_config.h

# Verify backward compat
cd firmware && grep -c '#warning.*BIBA_OPEN_LOOP is deprecated' include/biba_config.h

# Verify old BIBA_OPEN_LOOP define is REMOVED (unconditional define, NOT the backward compat block)
cd firmware && grep -n '^#define BIBA_OPEN_LOOP' include/biba_config.h || echo "OK: unconditional BIBA_OPEN_LOOP removed"

# Verify section header pattern
cd firmware && grep -c 'Feature:' include/biba_config.h
  </automated>
</verification>

<success_criteria>
- [ ] biba_config.h содержит минимум 17 feature секций с заголовками-комментариями (FEAT-01, FEAT-03)
- [ ] Каждая секция содержит тумблер + все параметры фичи (FEAT-03)
- [ ] 4 dependency #error проверки в конце файла (FEAT-04)
- [ ] BIBA_OPEN_LOOP backward compat с #warning (D-02)
- [ ] Старый безусловный #define BIBA_OPEN_LOOP удалён
- [ ] CURRENT_LIMITER=0 выдаёт #warning
- [ ] Модульные .h файлы (zc_detector, spectral, pi) имеют #ifndef guards
- [ ] Default build: SUCCESS
- [ ] Open-loop build (RPM_CLOSED_LOOP=0): SUCCESS
- [ ] Dependency violation (PI→DR): BUILD FAILS с понятным #error
- [ ] All-off build: SUCCESS
</success_criteria>

<output>
After completion, create `.planning/phases/12-feature-gating/12-01-SUMMARY.md`
</output>
