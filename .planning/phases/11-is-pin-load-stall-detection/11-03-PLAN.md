---
phase: 11-is-pin-load-stall-detection
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - firmware/src/poc/is_rpm_poc_main.cpp
  - scripts/is_poc_sweepraw.py
autonomous: true
requirements:
  - LOAD-02
must_haves:
  truths:
    - "SWEEPRAW2_WIN L header emits 6 tokens: idx t_ms duty_pct L vbat_raw ibat_raw"
    - "SWEEPRAW2_WIN R header still emits 5 tokens (unchanged)"
    - "Python parser extracts vbat_raw/ibat_raw from L header when parts[5] and parts[6] present"
    - "Python parser writes NaN for vbat_raw/ibat_raw on R windows or old firmware (< 7 tokens)"
    - "CSV output gains two new columns: vbat_raw and ibat_raw"
    - "Firmware builds without error: pio run -e rpico_rp2040_is_poc"
  artifacts:
    - path: "firmware/src/poc/is_rpm_poc_main.cpp"
      provides: "Extended SWEEPRAW2_WIN L header with vbat/ibat fields"
      contains: "vbat_raw"
    - path: "scripts/is_poc_sweepraw.py"
      provides: "Parser with vbat_raw/ibat_raw column support"
      contains: "vbat_raw"
  key_links:
    - from: "firmware/src/poc/is_rpm_poc_main.cpp"
      to: "biba_hal_adc_sample(BIBA_ADC_CHAN_VBAT)"
      via: "Serial.printf SWEEPRAW2_WIN L header"
      pattern: "BIBA_ADC_CHAN_VBAT"
    - from: "scripts/is_poc_sweepraw.py"
      to: "CSV columns vbat_raw, ibat_raw"
      via: "_write_csv and _parse_both_windows"
      pattern: "vbat_raw"
---

<objective>
Add VBAT and IBAT raw ADC values to the SWEEPRAW_BOTH firmware protocol and extend the Python parser to capture them in CSV output.

Purpose: Satisfy D-B1 through D-B4. With sensor not yet installed the values will be floating-pin noise — that is acceptable (D-B2). The columns must be present for future battery-sag characterisation.

Output: Modified firmware/src/poc/is_rpm_poc_main.cpp (extended L header), modified scripts/is_poc_sweepraw.py (parser + _write_csv extended). Firmware must build cleanly.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-is-pin-load-stall-detection/11-CONTEXT.md
@.planning/phases/11-is-pin-load-stall-detection/11-RESEARCH.md

<interfaces>
<!-- Extracted from firmware/src/poc/is_rpm_poc_main.cpp lines 1238–1310 -->
<!-- cmd_sweepraw_both() current L/R header format -->

Current L header (line 1293):
  Serial.printf("SWEEPRAW2_WIN %lu %lu %.1f L\n",
                (unsigned long)w, (unsigned long)t_now, duty * 100.0f);

Current R header (line 1296):
  Serial.printf("SWEEPRAW2_WIN %lu %lu %.1f R\n",
                (unsigned long)w, (unsigned long)t_now, duty * 100.0f);

ADC channel constants (target_config.h, verified from CONTEXT.md):
  BIBA_ADC_CHAN_VBAT = 2
  BIBA_ADC_CHAN_IBAT = 3

biba_hal_adc_sample signature:
  uint16_t biba_hal_adc_sample(uint8_t channel);

Python parser current header parse (_parse_both_windows, lines ~95–100 of is_poc_sweepraw.py):
  if line.startswith("SWEEPRAW2_WIN"):
      parts = line.split()
      current_chan = parts[4] if len(parts) >= 5 else "L"
      current_meta = {"idx": int(parts[1]), "t_ms": int(parts[2]),
                      "duty": float(parts[3])}

Python _write_csv current columns:
  w.writerow(["win_idx", "t_ms", "duty_pct", "sample_idx", "adc_raw"])
  for win in windows:
      for i, v in enumerate(win["samples"]):
          w.writerow([win["idx"], win["t_ms"], f"{win['duty']:.2f}", i, v])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend cmd_sweepraw_both() — sample VBAT/IBAT and add to L header</name>
  <files>firmware/src/poc/is_rpm_poc_main.cpp</files>

  <read_first>
    - firmware/src/poc/is_rpm_poc_main.cpp lines 1238–1320 (full cmd_sweepraw_both function)
    - .planning/phases/11-is-pin-load-stall-detection/11-RESEARCH.md §2.4 (extension design)
    - .planning/phases/11-is-pin-load-stall-detection/11-CONTEXT.md D-B1, D-B2
  </read_first>

  <action>
  In cmd_sweepraw_both(), immediately before the L channel capture (`adc_capture_burst(BIBA_ADC_CHAN_IS_LEFT, ...)`), add two ADC samples:
    uint16_t vbat_raw = biba_hal_adc_sample(BIBA_ADC_CHAN_VBAT);
    uint16_t ibat_raw = biba_hal_adc_sample(BIBA_ADC_CHAN_IBAT);

  These two lines are placed at the TOP of the per-window loop body, before the biba_hal_motor_pwm_left(duty) is set (to ensure minimal IS capture latency — place them right after duty is set, before any adc_capture_burst call).

  Exact placement: after `float duty = _sweep_duty(...)` and `biba_hal_motor_pwm_left(duty); biba_hal_motor_pwm_right(duty);` — insert the two sample lines there. This ensures both IS captures happen with motors already running at the new duty, and VBAT/IBAT are sampled once per window pair.

  Change the L header printf from:
    Serial.printf("SWEEPRAW2_WIN %lu %lu %.1f L\n",
                  (unsigned long)w, (unsigned long)t_now, duty * 100.0f);
  To:
    Serial.printf("SWEEPRAW2_WIN %lu %lu %.1f L %u %u\n",
                  (unsigned long)w, (unsigned long)t_now, duty * 100.0f,
                  (unsigned)vbat_raw, (unsigned)ibat_raw);

  Leave the R header printf unchanged (still 5-token format).

  Do not change any other logic in cmd_sweepraw_both() or elsewhere.

  After the edit, run:
    cd /home/ros2/Downloads/biba/firmware && pio run -e rpico_rp2040_is_poc 2>&1 | tail -5
  Confirm "SUCCESS" in output.
  </action>

  <verify>
    <automated>cd /home/ros2/Downloads/biba/firmware && pio run -e rpico_rp2040_is_poc 2>&1 | grep -E "SUCCESS|ERROR|error:" | head -5</automated>
  </verify>

  <acceptance_criteria>
  - grep -n "vbat_raw" firmware/src/poc/is_rpm_poc_main.cpp returns at least 2 lines (declaration + printf).
  - grep -c "vbat_raw" firmware/src/poc/is_rpm_poc_main.cpp prints a number >= 2.
  - grep -n "SWEEPRAW2_WIN.*L %u %u" firmware/src/poc/is_rpm_poc_main.cpp returns 1 match.
  - The R header printf line still matches: Serial.printf("SWEEPRAW2_WIN %lu %lu %.1f R\n"
  - pio run -e rpico_rp2040_is_poc exits 0 with "SUCCESS" in output (no compilation errors).
  </acceptance_criteria>

  <done>
  cmd_sweepraw_both() emits a 7-token L header per window and a 5-token R header.
  Firmware builds cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Python parser — vbat_raw/ibat_raw columns in CSV</name>
  <files>scripts/is_poc_sweepraw.py</files>

  <read_first>
    - scripts/is_poc_sweepraw.py lines 80–140 (_parse_both_windows and _write_csv functions)
    - .planning/phases/11-is-pin-load-stall-detection/11-RESEARCH.md §2.2 (current parser logic) and §2.4 (extension design)
    - .planning/phases/11-is-pin-load-stall-detection/11-CONTEXT.md D-B3, D-B4
  </read_first>

  <action>
  Two changes in scripts/is_poc_sweepraw.py:

  Change 1 — _parse_both_windows():
  After the existing `current_meta = {"idx": ..., "t_ms": ..., "duty": ...}` assignment in the
  SWEEPRAW2_WIN branch, add the conditional vbat/ibat extraction immediately after:

    if current_chan == "L" and len(parts) >= 7:
        current_meta["vbat_raw"] = int(parts[5])
        current_meta["ibat_raw"] = int(parts[6])
    else:
        current_meta["vbat_raw"] = float("nan")
        current_meta["ibat_raw"] = float("nan")

  The existing `current_chan = parts[4] if len(parts) >= 5 else "L"` line is already set before
  current_meta, so current_chan is available for the condition.

  Note: R windows will always get NaN (len(parts)==5 for R headers, per D-B4).
  Old firmware L headers (len(parts)==5) also get NaN (backward compat per D-B4).

  Change 2 — _write_csv():
  Extend the CSV header row from:
    w.writerow(["win_idx", "t_ms", "duty_pct", "sample_idx", "adc_raw"])
  To:
    w.writerow(["win_idx", "t_ms", "duty_pct", "sample_idx", "adc_raw", "vbat_raw", "ibat_raw"])

  Extend the per-sample row from:
    w.writerow([win["idx"], win["t_ms"], f"{win['duty']:.2f}", i, v])
  To:
    w.writerow([win["idx"], win["t_ms"], f"{win['duty']:.2f}", i, v,
                win.get("vbat_raw", float("nan")), win.get("ibat_raw", float("nan"))])

  No other changes to is_poc_sweepraw.py.
  </action>

  <verify>
    <automated>cd /home/ros2/Downloads/biba && grep -n "vbat_raw" scripts/is_poc_sweepraw.py | head -10</automated>
  </verify>

  <acceptance_criteria>
  - grep -c "vbat_raw" scripts/is_poc_sweepraw.py prints 6 or more (header row + data row + 2 parse branches + 2 nan assignments).
  - The CSV header row in _write_csv contains the exact string: "vbat_raw", "ibat_raw"
  - grep -n "len(parts) >= 7" scripts/is_poc_sweepraw.py returns 1 match.
  - grep -n 'float("nan")' scripts/is_poc_sweepraw.py returns at least 2 matches (the two NaN assignments in the parse branch).
  - python3 -c "import scripts.is_poc_sweepraw" exits 0 (no import-time syntax errors). Run from /home/ros2/Downloads/biba directory with PYTHONPATH=. or via: python3 -c "import importlib.util; s=importlib.util.spec_from_file_location('m','scripts/is_poc_sweepraw.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m)" 2>&1 | head -3
  </acceptance_criteria>

  <done>
  _parse_both_windows() extracts vbat_raw/ibat_raw from 7-token L headers; fills NaN for R windows
  and old-firmware L headers. _write_csv() emits 7-column rows. Parser is backward compatible.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware serial output→Python parser | Parser consumes firmware protocol lines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | Python parser int(parts[5]) | mitigate | Already guarded by len(parts) >= 7 check before int() call; invalid data stays NaN |
| T-11-03-02 | Denial of Service | ADC sample in ISR-adjacent code | accept | biba_hal_adc_sample() is a non-blocking single-shot call; 2 extra calls per window add ~10 µs overhead, negligible vs 102 ms window |
</threat_model>

<verification>
1. grep -n "vbat_raw" firmware/src/poc/is_rpm_poc_main.cpp  — expect ≥ 2 lines
2. cd firmware && pio run -e rpico_rp2040_is_poc  — expect SUCCESS
3. grep -n "vbat_raw" scripts/is_poc_sweepraw.py  — expect ≥ 6 lines
4. grep -n "len(parts) >= 7" scripts/is_poc_sweepraw.py  — expect 1 line
</verification>

<success_criteria>
- SWEEPRAW2_WIN L header prints 7 tokens; R header unchanged at 5 tokens.
- pio run -e rpico_rp2040_is_poc exits 0.
- is_poc_sweepraw.py _parse_both_windows extracts vbat_raw/ibat_raw from L headers; fills NaN otherwise.
- _write_csv writes 7-column CSV rows with vbat_raw and ibat_raw as the last two columns.
</success_criteria>

<output>
After completion, create .planning/phases/11-is-pin-load-stall-detection/11-03-SUMMARY.md
Include: exact diff summary, new SWEEPRAW2_WIN L header format, CSV column list.
</output>
