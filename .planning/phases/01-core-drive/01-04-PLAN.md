---
phase: 01-core-drive
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - firmware/src/modes/mode_standalone.c
autonomous: true
requirements:
  - MOTOR-03
  - SAFE-03

must_haves:
  truths:
    - "Two per-motor biba_ramp_t states declared static at file scope in mode_standalone.c"
    - "biba_ramp_init called for both states in biba_mode_standalone_init()"
    - "biba_ramp_reset called for both states on failsafe rising edge (D-04)"
    - "biba_ramp_reset called for both states on disarm edge (D-04)"
    - "biba_hal_ssr_set(armed) called immediately after s_armed = armed (D-10, Pitfall 5)"
    - "biba_ramp_update called for left_out and right_out BEFORE the melody/going_reverse checks (D-03, D-05, Pitfall 2, Pitfall 3)"
  artifacts:
    - path: "firmware/src/modes/mode_standalone.c"
      provides: "Full ramp + SSR wiring"
      contains: "biba_ramp_update(&s_ramp_left"
  key_links:
    - from: "mode_standalone_tick (post-mix armed block)"
      to: "biba_ramp_update"
      via: "left_out = biba_ramp_update(&s_ramp_left, left_out, dt);"
      pattern: "biba_ramp_update.*s_ramp_left.*left_out.*dt"
    - from: "mode_standalone_tick (s_armed assignment)"
      to: "biba_hal_ssr_set"
      via: "s_armed = armed; biba_hal_ssr_set(armed);"
      pattern: "biba_hal_ssr_set"
---

<objective>
Wire biba_ramp_t state machines and biba_hal_ssr_set into mode_standalone.c.
Four edits total: include, static declarations, init call, and three tick-function call sites.

Purpose: Completes MOTOR-03 (ramping in the control loop) and SAFE-03 (SSR on arm/disarm).
Output: mode_standalone.c modified — ramp and SSR are live in the standalone control loop.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-drive/01-CONTEXT.md
@.planning/phases/01-core-drive/01-RESEARCH.md
@.planning/phases/01-core-drive/01-01-SUMMARY.md
@.planning/phases/01-core-drive/01-02-SUMMARY.md
@.planning/phases/01-core-drive/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 — ramp interface -->
void  biba_ramp_init(biba_ramp_t *r);
void  biba_ramp_reset(biba_ramp_t *r);
float biba_ramp_update(biba_ramp_t *r, float target, float dt);

<!-- From Plan 02 — SSR HAL interface -->
void biba_hal_ssr_set(bool enabled);

<!-- Key existing code blocks in mode_standalone_tick() used as landmarks -->

/* --- BLOCK A: failsafe rising edge (add ramp_reset here) --- */
if (failsafe && !s_last_failsafe) {
    biba_melody_player_start(&s_player, &biba_melody_failsafe);
}   /* <-- add resets before this brace */
s_last_failsafe = failsafe;

/* --- BLOCK B: disarm edge (add ramp_reset here) --- */
} else if (!armed && s_armed) {
    printf("[biba] DISARMED\r\n");
    biba_pid_reset(&s_heading_pid);
    if (!failsafe) {
        biba_melody_player_start(&s_player, &biba_melody_disarm);
    }
    /* Exit trim mode on disarm edge (safety) */
    s_trim_mode_active = false;
}   /* <-- add resets before this brace */
s_armed = armed;   /* <-- add biba_hal_ssr_set(armed) on the NEXT line */

/* --- BLOCK C: post-mix → insert ramp calls AFTER this closing brace --- */
    /* Apply trim */
    if (trim > 0.0f) {
        right_out *= (1.0f - trim);
    } else if (trim < 0.0f) {
        left_out *= (1.0f + trim);
    }
}   /* <-- end of if (armed) block; add ramp calls after this */

/* --- BLOCK C continues: ramp calls go here, BEFORE this line --- */
/* If actively driving, motors are needed — interrupt melodies. */
bool control_active = armed && ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add include, static declarations, and biba_mode_standalone_init() calls</name>
  <files>firmware/src/modes/mode_standalone.c</files>
  <read_first>
    - firmware/src/modes/mode_standalone.c lines 1-170  — read the includes section, static declarations
      (s_armed, s_last_failsafe, etc.), and biba_mode_standalone_init() function body
    - firmware/src/app/ramp.h  — verify field names match what we're declaring
    - .planning/phases/01-core-drive/01-RESEARCH.md  — Integration Point 1 section
  </read_first>
  <action>
    Make three additions to mode_standalone.c:

    **Edit 1 — include**: After the `#include "app/failsafe.h"` line, add:
    ```c
    #include "app/ramp.h"
    ```

    **Edit 2 — static declarations**: In the file-scope static declarations block (near `static bool s_armed;`
    and `static bool s_last_failsafe;`), add after those two lines:
    ```c
    /* Per-motor slew-rate limiters (D-01, D-03). */
    static biba_ramp_t s_ramp_left;
    static biba_ramp_t s_ramp_right;
    ```

    **Edit 3 — init call**: Inside `biba_mode_standalone_init()`, after the existing
    `biba_failsafe_init(&s_crsf_failsafe, BIBA_CRSF_TIMEOUT_MS);` call, add:
    ```c
    biba_ramp_init(&s_ramp_left);
    biba_ramp_init(&s_ramp_right);
    ```
    Comment: This guarantees both states are zero before the first tick.

    IMPORTANT: `s_ramp_left` and `s_ramp_right` are `static` — they are zero-initialised
    by the C runtime anyway, but the explicit `biba_ramp_init` calls document intent and
    will be needed if the states are ever reset mid-session (e.g. mode switch).
  </action>
  <verify>
    <automated>grep -n "s_ramp_left\|s_ramp_right\|ramp.h" firmware/src/modes/mode_standalone.c | head -20</automated>
  </verify>
  <acceptance_criteria>
    - mode_standalone.c contains `#include "app/ramp.h"` in the includes section
    - mode_standalone.c contains `static biba_ramp_t s_ramp_left;` and `static biba_ramp_t s_ramp_right;`
    - biba_mode_standalone_init() contains both `biba_ramp_init(&s_ramp_left);` and `biba_ramp_init(&s_ramp_right);`
  </acceptance_criteria>
  <done>Include, declarations, and init calls added. mode_standalone.c compiles with ramp.h.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ramp resets, SSR call, and ramp update calls in mode_standalone_tick()</name>
  <files>firmware/src/modes/mode_standalone.c</files>
  <read_first>
    - firmware/src/modes/mode_standalone.c lines 201-513  — read the full tick function carefully;
      identify the exact line numbers for each of the four insertion points using the landmark
      code blocks described in the interfaces section above
    - .planning/phases/01-core-drive/01-RESEARCH.md  — Integration Points 2 and 3; Pitfalls 2, 3, 5
    - .planning/phases/01-core-drive/01-CONTEXT.md  — D-03, D-04, D-05, D-10 decisions
  </read_first>
  <action>
    Make four precise edits to `biba_mode_standalone_tick()`. Each edit has an exact landmark.

    **Edit A — Failsafe rising edge reset (D-04)**:
    Locate the block: `if (failsafe && !s_last_failsafe) { biba_melody_player_start(...failsafe...); }`
    Add inside the braces, AFTER the melody call and BEFORE the closing `}`:
    ```c
        biba_ramp_reset(&s_ramp_left);    /* D-04: hard reset on failsafe edge */
        biba_ramp_reset(&s_ramp_right);
    ```

    **Edit B — Disarm edge reset (D-04)**:
    Locate the block: `} else if (!armed && s_armed) { ... s_trim_mode_active = false; }`
    Add inside the braces, AFTER `s_trim_mode_active = false;` and BEFORE the closing `}`:
    ```c
        biba_ramp_reset(&s_ramp_left);    /* D-04: hard reset on disarm edge */
        biba_ramp_reset(&s_ramp_right);
    ```

    **Edit C — SSR follows arm state (D-10)**:
    Locate the line: `s_armed = armed;`
    On the very next line after it, add:
    ```c
    biba_hal_ssr_set(armed);              /* D-10: SSR HIGH=armed, LOW=disarmed */
    ```
    CRITICAL: Must be AFTER `s_armed = armed;` (Pitfall 5). If placed before, the edge
    detection on the next tick fires with a one-tick delay.

    **Edit D — Ramp update calls (D-03, D-05)**:
    Locate the closing `}` of the `if (armed) { ... }` block. This block ends with the trim
    application code:
    ```c
        } else if (trim < 0.0f) {
            left_out *= (1.0f + trim);
        }
    }
    ```
    Immediately AFTER this closing `}` (on the next line), BEFORE the `/* If actively driving */`
    comment and `bool control_active = ...` line, add:
    ```c
    /* D-03, D-05: Apply ramp post-mix. Always runs — never bypassed.
     * MUST be before melody/going_reverse checks (Pitfalls 2 and 3). */
    left_out  = biba_ramp_update(&s_ramp_left,  left_out,  dt);
    right_out = biba_ramp_update(&s_ramp_right, right_out, dt);
    ```

    VERIFY ORDERING after edits:
    1. `s_armed = armed;`          ← already exists
    2. `biba_hal_ssr_set(armed);`  ← Edit C
    3. `if (armed) { ... }`        ← already exists (mix + limiter + trim)
    4. `left_out  = biba_ramp_update(...);`  ← Edit D
    5. `right_out = biba_ramp_update(...);`  ← Edit D
    6. `bool control_active = ...` ← already exists
    7. `going_reverse = ...`        ← already exists
    8. `biba_bts7960_drive(...)`    ← already exists (inside !s_player.active)
  </action>
  <verify>
    <automated>grep -c "biba_ramp_update\|biba_ramp_reset\|biba_hal_ssr_set" firmware/src/modes/mode_standalone.c</automated>
  </verify>
  <acceptance_criteria>
    - mode_standalone.c contains exactly 2 occurrences of `biba_ramp_update` (left and right, in Edit D)
    - mode_standalone.c contains exactly 4 occurrences of `biba_ramp_reset` (2 in failsafe edge, 2 in disarm edge)
    - mode_standalone.c contains exactly 1 occurrence of `biba_hal_ssr_set`
    - `grep -n "biba_hal_ssr_set\|s_armed = armed" mode_standalone.c` — the ssr_set line appears AFTER the s_armed assignment line
    - `grep -n "biba_ramp_update\|control_active" mode_standalone.c` — the ramp_update lines appear BEFORE the control_active line
    - `grep -n "biba_ramp_update\|biba_bts7960_drive" mode_standalone.c` — ramp_update lines appear BEFORE biba_bts7960_drive line
  </acceptance_criteria>
  <done>Four edits applied. Ramp resets on failsafe/disarm edges. SSR follows arm state. Ramp updates applied post-mix before melody/drive checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| biba_ramp_update inputs | left_out/right_out are floats already bounded by current limiter; dt computed from uint32_t subtraction |
| biba_hal_ssr_set input | armed bool — value is always resolved before call; no race condition in single-threaded tick |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Ramp applied inside if(armed) block — ramp freezes disarmed | mitigate | Ramp calls placed OUTSIDE if(armed) per Edit D; disarm/failsafe resets guarantee state=0 on next arm |
| T-04-02 | Tampering | going_reverse check uses pre-ramp values | mitigate | Ramp calls (Edit D) placed before going_reverse check — post-ramp values used for reverse detection |
| T-04-03 | Elevation | SSR set before s_armed updated — edge logic one tick late | mitigate | Edit C places biba_hal_ssr_set after s_armed = armed per Pitfall 5 in RESEARCH.md |
| T-04-04 | Denial of Service | Melody freezes ramp (ramp inside !s_player.active block) | mitigate | Edit D places ramp calls before the !s_player.active block (Pitfall 2) |
</threat_model>

<verification>
```bash
# Verify all 4 edits landed
grep -n "biba_ramp_update\|biba_ramp_reset\|biba_hal_ssr_set\|#include.*ramp.h\|s_ramp_left\|biba_ramp_init" \
    firmware/src/modes/mode_standalone.c
```
Expected: ramp.h include, s_ramp_left/right declarations, ramp_init calls in init,
2x ramp_reset in failsafe block, 2x ramp_reset in disarm block, 1x ssr_set after s_armed=armed,
2x ramp_update before control_active.
</verification>

<success_criteria>
- mode_standalone.c modified with 4 edits (A-D above)
- Ordering: s_armed=armed → ssr_set → if(armed){...} → ramp_update → control_active → bts7960_drive
- 4 ramp_reset calls total (2 per edge: failsafe and disarm)
- 1 biba_hal_ssr_set call immediately after s_armed assignment
- 2 biba_ramp_update calls post-armed-block, pre-melody
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-drive/01-04-SUMMARY.md`
</output>
