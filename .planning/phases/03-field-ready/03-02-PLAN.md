---
phase: 03-field-ready
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - docs/variants.md
  - docs/system_architecture.md
  - docs/deployment.md
  - docs/wiring.md
  - docs/field-validation.md
  - artifacts/current-trace/README.md
  - artifacts/telemetry-captures/README.md
autonomous: true
requirements:
  - THERM-02
  - VARIANT-01
  - VARIANT-02
must_haves:
  truths:
    - "All supported hardware variants are listed in one canonical matrix with status and implementation link."
    - "Field validation procedure explicitly includes heat-sink installation evidence and artifact capture steps."
    - "Documentation aligns thermal reset wording to EN/INH path and 100 us pulse for brushed BTS7960 path."
  artifacts:
    - path: docs/variants.md
      provides: "Canonical variant matrix for Pi Zero 2W, RP2040, STM32F103"
    - path: docs/field-validation.md
      provides: "Phase 3 field-test checklist and evidence contract"
    - path: artifacts/current-trace/README.md
      provides: "Trace artifact naming and required metadata"
  key_links:
    - from: docs/variants.md
      to: firmware/targets/RPICO_RP2040/target.md
      via: "implementation link column"
      pattern: "RPICO_RP2040/target.md"
    - from: docs/field-validation.md
      to: artifacts/current-trace/
      via: "required evidence paths"
      pattern: "artifacts/current-trace"
---

<objective>
Create canonical Phase 3 documentation and evidence scaffolding for hardware variants and field readiness.

Purpose: Satisfy THERM-02 and VARIANT requirements with auditable docs that are directly tied to build targets/branches and field artifacts.
Output: One canonical variants matrix, one field-validation protocol, and artifact README contracts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-field-ready/03-CONTEXT.md
@.planning/phases/03-field-ready/03-RESEARCH.md
@README.md
@docs/system_architecture.md
@docs/deployment.md
@docs/wiring.md
@firmware/targets/RPICO_RP2040/target.md
@firmware/targets/BLUEPILL_F103C8/target.md
@firmware/targets/BIBA_F103_REV_A/target.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish canonical hardware variant matrix</name>
  <files>docs/variants.md, docs/system_architecture.md, docs/deployment.md</files>
  <read_first>README.md, docs/system_architecture.md, docs/deployment.md, firmware/targets/RPICO_RP2040/target.md, firmware/targets/BLUEPILL_F103C8/target.md, firmware/targets/BIBA_F103_REV_A/target.md</read_first>
  <action>Create `docs/variants.md` with a table containing columns `platform`, `board`, `motor_type`, `driver_type`, `optional_modules`, `status`, `implementation_link`. Populate at minimum rows for Pi Zero 2W, RP2040 standalone, STM32F103 standalone/companion. Set statuses using only `ready`, `WIP`, `planned`. Each row must contain one concrete link to `target.md`, compose file, or branch reference. Add backlinks to `docs/variants.md` from `docs/system_architecture.md` and `docs/deployment.md` and remove duplicate status tables from those files if present.</action>
  <verify>
    <automated>cd /home/ros2/Downloads/biba && python3 -c "import pathlib,re; t=pathlib.Path('docs/variants.md').read_text(encoding='utf-8'); lines=[ln for ln in t.splitlines() if ln.strip().startswith('|')]; assert len(lines)>=5,'Expected header + separator + at least 3 rows'; header=[c.strip().lower() for c in lines[0].strip().strip('|').split('|')]; assert header==['platform','board','motor_type','driver_type','optional_modules','status','implementation_link'],header; rows=[[c.strip() for c in ln.strip().strip('|').split('|')] for ln in lines[2:]]; required={r[0].strip().lower():r for r in rows if len(r)>=7 and r[0].strip().lower() in {'pi zero 2w','rp2040','stm32f103'}}; assert set(required)=={'pi zero 2w','rp2040','stm32f103'},'Missing one or more required platform rows'; allowed={'ready','wip','planned'}; assert all(required[k][5].lower() in allowed for k in required),'Invalid status in required rows'; assert all('](' in required[k][6] for k in required),'Missing markdown implementation link in required rows'; print('variants matrix checks passed')"</automated>
  </verify>
  <acceptance_criteria>`docs/variants.md` exists, has all required columns, includes all three platforms, and every row has status + implementation link.</acceptance_criteria>
  <done>VARIANT-01 and VARIANT-02 are satisfied by one reviewable canonical artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 3 field-validation protocol and thermal evidence checklist</name>
  <files>docs/field-validation.md, docs/wiring.md</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/03-field-ready/03-CONTEXT.md, docs/wiring.md, docs/telemetry-investigation-2026-03-28.md, docs/plans/2026-03-30-current-sense-calibration-trace-design.md</read_first>
  <action>Create `docs/field-validation.md` with sections: `hardware prerequisites`, `heat-sink installation evidence`, `30-minute drive protocol`, `abort criteria`, `required artifacts`, `pass/fail rubric`. In thermal reset section, document locked decisions D-01..D-04 exactly: EN/INH path, 100 us low pulse, PWM zero during reset. Update `docs/wiring.md` with a short pointer to the Phase 3 thermal reset behavior and this protocol file without introducing SSR as the reset mechanism.</action>
  <verify>
    <automated>cd /home/ros2/Downloads/biba && grep -n "EN/INH\|100 us\|PWM" docs/field-validation.md && grep -n "variants.md\|field-validation.md" docs/wiring.md</automated>
  </verify>
  <acceptance_criteria>Protocol includes explicit THERM-02 evidence checklist (plate photo/record + run metadata) and exact D-01..D-04 behavior text.</acceptance_criteria>
  <done>Field procedure is explicit, reproducible, and aligned to locked thermal decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Define artifact README contracts for field evidence folders</name>
  <files>artifacts/current-trace/README.md, artifacts/telemetry-captures/README.md</files>
  <read_first>biba-controller/main.py, scripts/vcp_capture.py, docs/field-validation.md</read_first>
  <action>Create `artifacts/current-trace/README.md` and `artifacts/telemetry-captures/README.md` specifying filename format, minimum metadata keys, and how each artifact maps to the pass/fail rubric in `docs/field-validation.md`. Include exact commands: `python3 scripts/vcp_capture.py --help` and the runtime env vars used by current trace (`MOTOR_CURRENT_TRACE_ENABLED`, `MOTOR_CURRENT_TRACE_PATH`).</action>
  <verify>
    <automated>cd /home/ros2/Downloads/biba && python3 -m pytest tests/test_vcp_capture.py tests/test_main.py -q</automated>
  </verify>
  <acceptance_criteria>Both artifact folders contain README contracts with command snippets and required metadata fields tied to protocol sections.</acceptance_criteria>
  <done>Evidence directories are self-describing and executable for field operators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation source -> operator action | Inaccurate docs can cause unsafe or unverifiable field behavior |
| artifact producers -> validation report | Missing metadata can invalidate safety claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | docs/field-validation.md | mitigate | Encode D-01..D-04 behavior exactly and forbid SSR reset wording for this path |
| T-03-05 | R | artifacts/current-trace/README.md | mitigate | Require artifact naming + metadata schema to make field claims auditable |
| T-03-06 | I | docs/variants.md | mitigate | Require one implementation link per row to preserve integrity of readiness status |
</threat_model>

<verification>
- `cd /home/ros2/Downloads/biba && python3 -m pytest tests/test_config.py tests/test_vcp_capture.py tests/test_main.py -q`
- `cd /home/ros2/Downloads/biba && grep -n "ready\|WIP\|planned" docs/variants.md`
</verification>

<success_criteria>
- Canonical variant matrix exists and covers Pi Zero 2W, RP2040, STM32F103 with status and implementation link.
- Field protocol and artifact contracts define THERM-02 evidence requirements.
- Thermal reset narrative in docs matches D-01..D-04.
</success_criteria>

<output>
After completion, create `.planning/phases/03-field-ready/03-02-SUMMARY.md`.
</output>
