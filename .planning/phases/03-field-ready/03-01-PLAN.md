---
phase: 03-field-ready
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - firmware/include/biba_config.h
  - firmware/src/hal/biba_hal.h
  - firmware/src/hal/biba_hal.c
  - firmware/src/hal/biba_hal_rp2040.c
  - firmware/src/drivers/bts7960.h
  - firmware/src/drivers/bts7960.c
  - firmware/src/modes/mode_standalone.c
  - firmware/test/test_bts7960/test_main.c
autonomous: true
requirements:
  - THERM-01
must_haves:
  truths:
    - "Arm edge runs BTS7960 EN/INH reset sequence (D-01, D-02, D-04)."
    - "Reset pulse keeps EN low for 100 us and does not target datasheet minimum (D-03)."
    - "PWM output remains zero while EN is low and until re-enable completes (D-04)."
    - "Software treats reset as best-effort and does not assume instant thermal recovery (D-05)."
  artifacts:
    - path: firmware/src/drivers/bts7960.c
      provides: "Thermal-latch reset primitive using enable lines only"
    - path: firmware/src/modes/mode_standalone.c
      provides: "Arm-state integration for reset sequencing"
    - path: firmware/test/test_bts7960/test_main.c
      provides: "Regression tests for EN pulse + zero-PWM invariant"
  key_links:
    - from: firmware/src/modes/mode_standalone.c
      to: firmware/src/drivers/bts7960.c
      via: "arm transition calls thermal reset primitive"
      pattern: "biba_bts7960_thermal_reset"
    - from: firmware/src/drivers/bts7960.c
      to: firmware/src/hal/biba_hal.h
      via: "microsecond delay and enable writes"
      pattern: "biba_hal_delay_us|biba_hal_(left|right)_enable"
---

<objective>
Implement the Phase 3 BTS7960 thermal-latch reset contract in firmware and wire it into arm initialization.

Purpose: Deliver THERM-01 thermal-recovery behavior per locked decisions D-01..D-05 without using SSR for latch reset.
Output: Driver/HAL reset primitive, arm-edge integration, and native firmware regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-field-ready/03-CONTEXT.md
@.planning/phases/03-field-ready/03-RESEARCH.md
@firmware/src/drivers/bts7960.h
@firmware/src/drivers/bts7960.c
@firmware/src/modes/mode_standalone.c
@firmware/test/test_control_loop/test_main.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EN/INH thermal-reset primitive and 100 us config constant</name>
  <files>firmware/include/biba_config.h, firmware/src/hal/biba_hal.h, firmware/src/hal/biba_hal.c, firmware/src/hal/biba_hal_rp2040.c, firmware/src/drivers/bts7960.h, firmware/src/drivers/bts7960.c</files>
  <read_first>firmware/src/drivers/bts7960.h, firmware/src/drivers/bts7960.c, firmware/src/hal/biba_hal.h, firmware/include/biba_config.h, .planning/phases/03-field-ready/03-CONTEXT.md</read_first>
  <action>Add `#define BIBA_BTS7960_RESET_PULSE_US 100u` to `firmware/include/biba_config.h`; add `void biba_hal_delay_us(uint32_t us);` to `firmware/src/hal/biba_hal.h`; implement `biba_hal_delay_us` in `firmware/src/hal/biba_hal.c` and `firmware/src/hal/biba_hal_rp2040.c`; add `void biba_bts7960_thermal_reset(uint32_t pulse_us);` declaration in `firmware/src/drivers/bts7960.h` and implementation in `firmware/src/drivers/bts7960.c` that executes: PWM left/right to zero, both enables LOW, `biba_hal_delay_us(max(pulse_us, BIBA_BTS7960_RESET_PULSE_US))`, both enables HIGH. Do not use SSR controls in this primitive (per D-01).</action>
  <verify>
    <automated>cd firmware && pio run -e rpico_rp2040_standalone -e biba_f103_rev_a_standalone</automated>
  </verify>
  <acceptance_criteria>Build succeeds for RP2040 and standalone environments, symbol `biba_bts7960_thermal_reset` is exported, and reset pulse floor is 100 us.</acceptance_criteria>
  <done>Thermal-reset primitive exists at driver/HAL owner level and is parameterized by explicit microsecond pulse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire reset sequence into arm initialization with zero-PWM gate</name>
  <files>firmware/src/modes/mode_standalone.c</files>
  <read_first>firmware/src/modes/mode_standalone.c, firmware/src/drivers/bts7960.h, .planning/phases/03-field-ready/03-CONTEXT.md</read_first>
  <action>In `biba_mode_standalone_init` and the arm rising-edge block in `biba_mode_standalone_tick`, call `biba_bts7960_thermal_reset(BIBA_BTS7960_RESET_PULSE_US)` before enabling non-zero drive output. Ensure the output path keeps left/right duty at 0 when reset is active and only resumes normal ramp/mix flow after reset completion. Keep existing SSR calls for unrelated failsafe behavior untouched, but do not use SSR to clear thermal latch (D-01, D-04).</action>
  <verify>
    <automated>cd firmware && pio run -e rpico_rp2040_standalone</automated>
  </verify>
  <acceptance_criteria>Arm transition performs EN-low pulse at least 100 us, no non-zero PWM call occurs between EN-low start and EN-high completion, and code comments/tests mark reset as best-effort rather than guaranteed immediate recovery (D-05).</acceptance_criteria>
  <done>Arm flow now enforces D-02/D-03/D-04 sequencing using BTS7960 enable lines only and keeps D-05 behavior explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add firmware regression tests for thermal reset sequencing</name>
  <files>firmware/test/test_bts7960/test_main.c</files>
  <read_first>firmware/test/test_control_loop/test_main.c, firmware/src/drivers/bts7960.c, firmware/src/hal/biba_hal.h</read_first>
  <action>Create `firmware/test/test_bts7960/test_main.c` with HAL fakes that record call order for `biba_hal_motor_pwm_left/right`, `biba_hal_left_enable`, `biba_hal_right_enable`, and `biba_hal_delay_us`; add tests that assert sequence exactly: PWM zero -> EN low -> delay_us(>=100) -> EN high; add test for explicit larger pulse argument preserving caller value above 100 us.</action>
  <verify>
    <automated>cd firmware && pio test -e native_test -f test_bts7960 -f test_control_loop</automated>
  </verify>
  <acceptance_criteria>New native tests fail before implementation and pass after implementation; no regression in `test_control_loop`.</acceptance_criteria>
  <done>Thermal reset contract is protected by deterministic native tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control-loop -> motor driver | Untrusted or stale control state must not produce unsafe actuation during reset |
| driver -> HAL GPIO/PWM | Low-level call ordering controls whether bridge is safely disabled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | firmware/src/modes/mode_standalone.c | mitigate | Call `biba_bts7960_thermal_reset` on arm edge and hold outputs at zero until completion |
| T-03-02 | D | firmware/src/drivers/bts7960.c | mitigate | Force PWM zero before EN low and enforce minimum 100 us low pulse in code |
| T-03-03 | R | firmware/test/test_bts7960/test_main.c | mitigate | Record exact HAL call sequence in native tests to provide reproducible evidence |
</threat_model>

<verification>
- `cd firmware && pio run -e rpico_rp2040_standalone -e biba_f103_rev_a_standalone`
- `cd firmware && pio test -e native_test -f test_bts7960 -f test_control_loop`
</verification>

<success_criteria>
- Locked decisions D-01..D-04 are implemented in code-path behavior.
- No SSR-based latch-reset action is introduced.
- Native tests validate zero-PWM and 100 us EN-low reset invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/03-field-ready/03-01-SUMMARY.md`.
</output>
